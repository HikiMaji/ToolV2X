# ToolV2X 用户需求

## 最新调整：GoT 可替换，先完成驾驶端具体设计（2026-09-14）

- 用户明确：“可以继续，同时依据后续情况，如果有更好的方案或者自创、拼接方案，其实 GoT 并不是必须保留的”。后续不得把保留 GoT 当作不可变约束，也不能把这句话解释成已经选定某个替代网络。
- 允许比较改造 GoT、结构化协同规划器及有依据的组合方案。选择依据是 P 状态/历史与 F 未来信息的实际使用、可训练性、完整任务条件和公平评价；不以模型更大或模块更多为目标。
- 本轮继续完成论文/源码映射与具体架构建议，不启动训练、新 GoT 生成或新方法实验。原有 P/F、真实返回、因果权限、最多两次调用和 RSU/I 后置等要求继续有效。
- 9-12-1/9-12-2 中“同一 GoT 修订”的组件绑定可在新版本中改为“同一共享驾驶模型修订”；请求执行、已购证据、因果边界和实验控制不能随之取消。旧 GoT 路径及实验仍按其原版本保存。
- 新建议见 [结构化协同规划器设计](structured_cooperative_planner_design_2026_09_14.md)。下文“保留原 GoT”是历史约束，已被本条更新；本轮没有修改运行代码或自动提交/推送。

## 最新主线约束：创新必须服务远端能力调用（2026-09-12）

- 用户在追问候选是否符合原始定义后明确确认：“是的，创新需要符合我们的主线”。研究问题继续是：本车根据当前驾驶任务与已有证据，决定是否调用邻车的哪项能力、请求什么，以及如何利用返回结果调整后续调用。依据为 [原始定义](../start.md) 与 [已批准框架](framework_design.md)。
- 初始选择保留 STOP、P、F；P→F、F→P 都可以作为条件分支，不固定先 F，也不预设两次调用一定优于一次。具体机制尚未锁定，不能把本次主线确认写成对某个算法的批准。
- [预测补证与配对收益学习候选](innovation_design_2026_09_12.md) 降为整体框架内的局部候选。预测细化、历史补充、表示压缩和接收器适配只有在服务上述调用决策时才纳入；不能由这些局部问题替换整个 ToolV2X 研究问题。
- 后续候选逐项说明：对应什么驾驶需求、为什么需要远端执行该任务、返回证据如何改变下一步决策，以及相对固定调用、单轮打包和同信息本地计算的实际增量。这些是设计筛选标准，不是已经成立的创新点。
- 两车使用同一个 MTR 不取消 F 的远端能力属性：调用前邻车可利用本车尚未获取的私有观测与上下文。完整 P 传输后相同上下文的本地重算，则需要单独核算信息与计算上的增量。
- 保留原 GoT/MTR、时间 t 因果输入、首版最多两次调用及 RSU/I 后置。现有基础训练按既有安排推进；本次文档校正没有修改接口、训练配置或启动新方法实验。

## 继续寻找主线创新，允许合理新增机制（2026-09-12）

- 用户明确“可以，开始寻找（也可以新增合理机制）”，授权在主线内继续文献、源码与方法研究。可新增请求参数、任务执行方式及学习机制；保留上面的两车、因果输入、原模块复用和 RSU/I 后置要求。
- 本轮综合报告为 [主线创新筛选](mainline_innovation_search_2026_09_12.md)，附协议、驾驶端和反方三份独立评议。MATE 已覆盖候选轨迹的远端评价，故最终候选收窄为“实际首次返回引起 GoT 方案修订后，以旧/新方案关系组织第二次 P/F 请求”。初始 STOP/P/F 继续保留。
- 上下文委托与历史增量的配对监督作为学习支撑；条件程序在 peer 内执行作为强单轮对照，不单列创新。须比较新方案单走廊、同输入普通模型与信息/成本公平的一轮方案；实际两次调用可能需要三次 GoT，全部计费。
- 上述是本轮研究建议，用户尚未锁定具体方法；未运行新模型、生成新效用标签或修改既有训练。不能把未检出完全相同近邻写成“首次”，也不能把新增机制许可扩成 RSU、条件反应预测或世界模型项目。

## 最新实施授权：完整框架补齐（2026-09-11）

- 用户在完整框架对照与修订说明后要求“开始进行”，授权按该整体方向推进实现、必要的真实运行、训练接入和评价；不再重复询问同一实施意图。
- 执行规格为 [framework_implementation_v1.md](framework_implementation_v1.md)。原 GoT/MTR、两车 P/F 和因果边界继续保留；先以统一执行器和明确规则调度形成完整基线，最终学习机制仍未锁定。
- 既有未提交工作和运行产物保留；本轮不是只完成 Ego，也不是把固定 F 的收益作为代码正确性的必要条件。

## 最新纠正：先形成完整可训练框架，不是只完成 Ego（2026-09-11）

- 用户明确解释，基础不只指 Ego：需要参考其他工作的完整框架，避免整体仍是半成品时持续推进局部数据和结果。
- 当前优先对照 DriveAgent-R1、V2V-GoT 的完整推理、训练和评价，补齐 ToolV2X 的关键运行链。不能把该要求再次缩成只训练 Ego，也不能直接沿用固定 F 增益表作为下一主要交付。
- 本轮对照与修订建议见 [framework_completeness_review_2026_09_11.md](framework_completeness_review_2026_09_11.md)。已有因果数据、原模型、报文与标签保留；最终样本组织应服从整体框架，当前固定 Ego/F 样本不是调用策略监督数据。
- 此纠正不等于用户已确定具体 RL、学习调度或新骨干。现有两车范围、原模块复用、因果输入、RSU 后置及常规事项无需重复审批的约束继续适用。

## 最新继续授权：执行收缩方案的下一步（2026-09-11）

- 用户在收缩方案及逐步研究判断原则之后要求“开始下一步”。本轮先执行 Ego / 固定 F 的配对样本准备，按当前方案完成必要实现、真实证据物化和验证。
- 直接六点轨迹、共享驾驶权重和冻结 MTR 是本轮设置；最终工具选择机制仍未确定。此前“本轮只做规划”指相应历史轮次，不限制本次执行。
- 本步已完成：2987 个训练帧、108 个验证帧的真实冻结 MTR 输入，导出 5974 条训练样本与 216 条验证输入；40 个跨已知断点的训练窗口单独停用。报告见 [paired_driving_data.md](paired_driving_data.md)。尚未进行共享驾驶适配或生成新的自车规划轨迹。

## 最新执行原则：逐步判断，并服务后续研究与论文（2026-09-11）

- 用户认可当前方案具有较完整的框架，但要求每一步进行时继续思考：为什么这样做、是否有优化空间、是否符合方案要求，以及是否有利于后续优化和论文撰写。
- 收缩后的配置是需要说明理由、可随证据调整的工作选择。逐步检查其科学合理性，不机械执行，也不把所有可选优化同时加入当前任务。
- 每一步在现有工作记录中简要说明目的、选择理由与代价、方案约束、可复用产物及论文结论边界；完成后按实际结果决定继续或局部调整。常规判断由 agent 执行，不增加逐步审批流程。
- 影响因果输入、公平比较或当前结论有效性的问题在当步解决；其他优化保留触发条件。论文表述跟随证据，正负结果均保留，不以方便叙事为由改变评价口径。

## 最新纠正：先收缩方案，再做规划（2026-09-11）

- 用户明确指出“你现在想做的太多了，重新想一想方案，再做规划”。当前先停止扩展实验与实现任务，重新确定单一主线；此前的继续授权不作为自动展开全部旧任务清单的依据。
- 本轮推荐见 [scope_reset_2026_09_11.md](scope_reset_2026_09_11.md)：固定已有驾驶初始化、直接轨迹输出和冻结 MTR，下一阶段只建立共享驾驶模型的 Ego / 固定 F 配对对照。具体配置仍是推荐方案，不写成用户已确认的最终 ToolV2X 机制。
- 本轮只更新规划，没有启动驾驶训练、数据处理、模型推理或新资源下载。

## 此前继续授权：稳定性复核与驾驶端推进（2026-09-11）

- 用户在下一轮方案（MTR 稳定性复核、驾驶端初始化/解码方式对照、共享驾驶模型适配和四动作配对评价）及纯 Ego 实现说明后要求“那开始下一步吧”，按已说明的顺序推进。
- 当前先执行 MTR 完整/交替上下文的等更新预算、多种子对照，协议见 `mtr_stability_plan.md`。具体种子与固定更新块是本次实验设置，不写成原 CMP 机制或最终 ToolV2X 方法。
- 下文“本轮不训练驾驶模型”等指对应历史轮次；最终查询策略、RSU/I 与闭环仍按既有顺序后置。

## 最新执行授权：原 MTR 因果适配（2026-09-10 至 11）

- 用户在原 MTR 离线目标标签、原损失训练、适配前后评价及已有 Q8/Q9 基础质量检查的下一步说明后回复“开始”，已授权执行这些工作。下文各历史阶段的训练 HOLD/零优化步不限制这次授权。
- 使用原 CMP 预测分支及已有权重，感知/跟踪缓存固定，GT 只用于独立离线监督和评价。本轮不训练驾驶模型或查询策略，RSU/I 仍后置。
- 当前实现采用完整/ROI 上下文交替的单次适配运行，这是本轮实现选择，不能表述为 CMP 原实验或用户已确定的最终机制。用户追问“你现在是怎么做的实验”时已说明数据、损失、上下文设置、验证选模与预训练来源限制。
- 协议见 `mtr_adaptation_plan.md`，真实过程见 `outputs/mtr_adaptation_v1/run_config.json` 和逐轮报告；已有驾驶回答的诊断见 `planning_quality.md`。新的训练/评价结果与历史准备阶段区分记录。

更新日期：2026-09-10。

## 阶段 A：已确认

- 项目目录：`/root/autodl-tmp/ToolV2X`。
- 按 `start.md` 中 ToolV2X 方向开始研究，使用 research[START] 路由至阶段 A。
- 目前不用考虑硬件和数据资源限制。
- 用户指定增加 DriveAgent-R1，纳入参考论文与优先精读清单。
- 用户在下载与提取完成后明确授权开始分析。

## 输入方案，尚未视为已验证结论

- 规划条件化序贯远端能力获取；首版感知 P、预测 M 两类工具。
- 轻量控制器，反事实效用监督，先做工具价值探查。
- V2V-GoT / V2V4Real 为候选底座，CARLA / V2XVerse 为候选闭环验证平台。
- 原文一个月工期、CCF-B 强度及直接进入实现的判断是输入材料中的建议，本轮未额外确认或验证。

## 2026-09-07 历史流程状态

- 已完成初步检索、18 篇论文下载与正文提取；已完成四篇关键论文的核心段落分析及相关近邻核查，见 `docs/analysis_report.md`。
- 新的 RQ 与方向收缩建议仍待确认；不将分析建议写成用户已确认需求。
- 尚未确认 Part 1，不进入阶段 B；未启动训练或模型推理。

## 2026-09-10 已确认与当前边界

- 用户要求理解前 agent 成果、判断下一步，并同意先检查/修正输入与评价协议，形成可执行四配置实验规范，再判断训练条件。
- 已完成本次检查和局部修正，见 `protocol_audit.md`、`experiment_protocol.md`。
- 当前正式训练 HOLD，原因是原 CMP 的 GT 依赖、远端独有目标缺失、检测器训练来源与真实消息协议尚未解决；不以硬件/数据资源限制替代研究判断。
- 新因果窗口已生成并验证。尚未完成因果 MTR 接入、四配置模型实验或 LLM 路由训练。

## 最新推进范围

- 用户明确纠正：先推进现在的车车工作，RSU 以后再说。本轮不增加 RSU 或更换到车路数据集。
- 机制仍未最终确定，P/F/I 和序贯查询是讨论中的候选，不能把建议当作已确认完整方法。
- 当前已完成车车单场景恒速接口验证和同信息对照，见 `vehicle_probe.md`；不等于学习型预测、路由有效性或闭环实验完成。

## 本次继续指令与执行状态

- 用户要求“开始下一步”，承接既有车车 P/F 能力验证；未增加 RSU、I 或最终机制确认。
- 已按这一范围完成共享线性残差预测基线的拟合、P_state/P_history/F 对照及两个验证场景评估，见 `learned_capability.md`。该模型未稳定超越恒速，未进入路由训练；不是 MTR 或最终预测器已确定。
- 早期“未训练”描述只适用于对应历史阶段；本轮已实际拟合该小型基线，旧 CMP 四配置正式训练仍未启动。

## 最新纠正：核心能力必须复用已有框架

- 用户明确指出：自行另写的小预测器不满足项目推进要求，至少应使用 CMP 或 V2V-GoT 原框架的模块，或在这些模块上修改。
- 后续以真实原模型、已有权重和必要的数据适配为主线。恒速/岭回归仅作为接口检查或简单对照，其负结果不能充当 ToolV2X 核心能力验证结论。
- 当前优先接入 CMP 的原 MotionTransformer 编码器/解码器及已有单车配置权重，保留因果输入要求；这不等于重新确定 CMP 完整融合架构为最终方法。

## 当前请求：设计完整框架

- 用户提出先搭较完整的框架再看数据是否更科学，随后明确要求“设计一下框架”。
- 本次输出为 [framework_design.md](framework_design.md)：从因果数据、原模型 P/F、消息接收、驾驶任务输出到配对评价的设计草案。
- “V2V-GoT 原驾驶模型 + CMP MTR 能力组件”、P 默认包含可用观测历史、最多两次调用等是本次推荐设计，不能写成用户已确认的最终机制。
- 本轮没有启动该框架的编码、训练或规划推理。后续的研究判断应基于原模型驱动的完整任务链与强固定对照；此前恒速/岭回归诊断不能作为方向否定依据。

## 框架设计后的实施授权

- 用户在阅读设计和纯文本重发后明确“OK，现在开始”，中断后再次要求“继续”。按该框架进入实现，无需重复确认同一设计。
- 实施计划见 `planning_implementation.md`。先接通原 V2V-GoT 任务端和真实 P/F 数据流；最终学习查询机制仍由后续对照决定。

## 最新资源分工与执行状态

- 用户提示 `cmp_repo_notes.md` 有 CMP 调研，可作为接入参考；其中历史资源缺项已用当前盘点更正。
- 用户允许调用 subagent 进行资源盘点/下载，指定 GPT-5.6-Luna Max。
- github.com、githubusercontent.com、githubassets.com、huggingface.co 相关资源由用户自行下载；agent 应提供明确链接、文件和目标路径，不通过子域名、镜像或跳转绕过分工。
- 收到该要求后，先前启动的下载已停止；已找到本地完整 LLaVA/CLIP 历史缓存，链接后离线加载成功，目前没有必须补下的文件。盘点见 `resource_downloads.md`。
- 原模型固定动作任务链已在一个真实验证帧运行，Ego/P/F/PF 均完成 Q8→Q9。实现/验证见 `planning_connection.md`；没有训练新的共享驾驶模型、学习查询器或开展正式规划质量/闭环评价。

## 继续推进证据表示和训练准备

- 用户在上述进展与下一步说明后要求“开始”，按已有授权推进紧凑证据和共享驾驶模型适配训练准备。
- 已完成可还原紧凑格式、原模型真实对照、3027 个训练帧/108 个验证帧的因果输入索引和独立监督标签，以及一个真实训练帧四动作共 8 条监督前向检查，见 `evidence_adaptation.md`。
- 没有优化参数或生成新训练权重。紧凑格式增加目标后仍有 Q9 格式失败，默认 JSON 保留；全量工具证据和正式适配训练仍待完成。
- 下一步沿已批准训练顺序先适配原 CMP MTR，再生成全量工具证据并训练共享 V2V-GoT 任务模型；不因当前弱模型或编码检查结果否定方向，不提前确定查询机制。

## GitHub 首次审查

- 用户要求盘点上传范围，随后提供 `https://github.com/HikiMaji/ToolV2X` 作为目标仓库，授权按已说明的清单首次推送。
- 目标是已有的公开空仓库；本轮未改变可见性。源码、相关原框架源码/配置、文档和精选证据已推送至 `main`，模型、完整数据、缓存和访问凭据不进入仓库。
- 核心路径可配置，提供独立轻量测试入口与全仓库审查提示。完整数据/权重仍由本地提供，发布不代表原实验可在网页环境直接完整重放。
- 对指定项目仓库的 Git 访问属于本次授权；其他 GitHub/Hugging Face 资源下载仍按之前分工由用户处理。本轮未发起远端审查评论或 PR。
# 2026-09-11 补充：先做可解释的 64 帧验证

- 用户说目前尚未完全理解正在做的工作，同意先验证。按此前建议检查 8 个训练录制组各 8 帧的数据、工具证据覆盖和模型实际输入，并用鸟瞰图解释。
- 沿用冻结后的原 CMP MTR 和原 GoT/V2V-LLM，保留真实 Q8 成功与失败。此次不训练新参数、不加入 RSU 或查询机制。
- 固定规则及结论边界见 `training_evidence_audit_plan.md`；通过检查仅支持后续输入/训练准备，不视为协作驾驶收益。

本次 64 帧与 512 次真实 Q8 已完成，结果见 `training_evidence_audit_results.md`。数据跳变、接收表示及公平监督的修正顺序是基于本次证据的下一步建议；不将它扩写成用户已确定的最终 ToolV2X 机制。没有新增驾驶训练、RSU、I 或闭环。

## 2026-09-12：取消本次训练提效，保持原训练

用户说“不用了”，取消的是刚安排的 4090 提效工作；保持当前正式驾驶模型适配继续。已停止自动测速/切换等待进程，未停止原训练，未启用候选回答位置输出头。后续不按旧提效计划自动重新启动交接，不将本次取消解释为终止基础训练或全部项目。

## 2026-09-12 上午：重新授权加速重跑与保存修复

用户得知训练中断且没有检查点后明确要求“你在加速跑吧”，授权修复定期保存、断点恢复，并实测加速后重新启动原适配。复用已完成数据，保持原模型、训练目标和有效批量。此要求取代上面的提效取消；旧交接程序继续作为历史记录，不恢复它，采用新运行目录。

## 2026-09-12：围绕 CCF-B/C 正式论文重新核对推进路径

- 用户要求再研究相关论文，并可检查其代码、数据及与论文描述的差异，主要目的是理解当前实验结果出来后如何形成自己的正式研究论文。
- 目标是一篇 CCF-B 或 C 论文用于毕业，会议和期刊均可，今年或明年均可，不要求追赶近期截稿；学校使用的目录版本尚未明确。
- 用户明确允许本次调研使用子 agent，并允许自行选择模型。此授权针对研究对照，不自动改变此前 GitHub/Hugging Face 大资源由用户下载的分工。
- 本次完成三组独立调研与主 agent 交叉复核，报告为 [paper_path_review_2026_09_12.md](paper_path_review_2026_09_12.md)。当前训练继续，没有因调研更换训练配置或启动新方法实验。
- 主报告提出的机制、对照和投稿方向是建议，不视为用户已经锁定最终方法。下一阶段不默认训练序贯 P→F：先解释充分适配后的 P/F 信息与质量—成本差异，再判断具体研究增量。

## 2026-09-12：研究关键创新点

- 用户指出当前缺少关键创新，并要求“研究一下，怎么设一个几个关键创新点”，授权继续具体方法设计与近邻核查。
- 本轮交付 [创新设计候选](innovation_design_2026_09_12.md)：预测细化与目标级历史补证的协议，以及通过四种输入的配对结果学习替代/互补收益的算法草案；普通数值接收器与正确的来源增补作为支撑。
- 上述推荐尚未成为用户确认的最终机制，没有修改当前训练或启动新方法实验。当前训练可复用为底座，但其五条件 SFT 数据不能冒充新选择器的配对效用标签。
- 保持两车 P/F、原 GoT/MTR 和最多两次调用；RSU/I 后置。候选要与完整 F、直接 P、P 本地重算、同监督普通选择器及单轮打包比较，不预设序贯必要或宣称创新已成立。

## 2026-09-12 傍晚：停止训练，先测已保存结果

- 用户明确要求“先不训练了，现在先花这么长时间训练有点不够值得，先测测结果”。此要求取代此前继续三轮驾驶适配的运行安排；本次停止训练，不自动续训。
- 已停止训练管理器与训练进程，保留第一轮完整检查点和最近两份周期恢复点。评估选用已完成第一轮验证的 `checkpoint-epoch01`，不把第二轮未保存的更新算入该模型。
- 本轮先按既有入口，在两个验证录制组各均匀选择 16 帧，比较同一检查点下 Ego/P/F/PF/规则的实际生成轨迹、失败率和配对差异。原 CMP MTR 响应与模型输入复用已有因果准备结果；不训练参数、不改机制。
- 这是 32 帧开发集诊断，不等于全验证集、独立测试集或论文主实验完成。先根据结果诊断后续工作，不因训练损失下降直接认定协同收益。

## 2026-09-12：继续深化核心机制，先核对主线

- 用户明确同意“继续深化核心机制”，并要求首先判断 ToolV2X 主线还能否保留。
- 本轮按原定义继续任务条件化的真实远端 P/F 调用及返回后的后续决策，形成 [核心机制 v0.1](core_mechanism_v0_1_2026_09_12.md)。主线符合原定义，不等于当前方案已经足够新或有效。
- 本次推荐一个本车请求价值模块、固定 peer 检索执行器、实际 GoT 驾驶反馈；首轮 STOP/P/F，次轮考虑当前方案或交互关系变化的 P/F 请求。peer 学习排序和额外归因头后置。具体算法与几何/容量参数仍为候选，不能写成用户逐项确认。
- 本轮为机制设计与代码/文献核对，不启动训练或模型推理；“方案修订关系”允许被公平对照否定，不将其与 ToolV2X 主线绑定。
