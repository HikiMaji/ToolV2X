# ToolV2X 用户需求

更新日期：2026-09-10。

## 阶段 A：已确认

- 项目目录：`/root/autodl-tmp/ToolV2X`。
- 按 `start.md` 中 ToolV2X 方向开始研究，使用 research[START] 路由至阶段 A。
- 目前不用考虑硬件和数据资源限制。
- 用户指定增加 DriveAgent-R1，纳入参考论文与优先精读清单。
- 用户在下载与提取完成后明确授权开始分析。

## 输入方案，尚未视为已验证结论

- 规划条件化序贯远端能力获取；首版感知 P、预测 M 两类工具。
- 轻量控制器，反事实效用监督，先做工具价值探查。
- V2V-GoT / V2V4Real 为候选底座，CARLA / V2XVerse 为候选闭环验证平台。
- 原文一个月工期、CCF-B 强度及直接进入实现的判断是输入材料中的建议，本轮未额外确认或验证。

## 2026-09-07 历史流程状态

- 已完成初步检索、18 篇论文下载与正文提取；已完成四篇关键论文的核心段落分析及相关近邻核查，见 `docs/analysis_report.md`。
- 新的 RQ 与方向收缩建议仍待确认；不将分析建议写成用户已确认需求。
- 尚未确认 Part 1，不进入阶段 B；未启动训练或模型推理。

## 2026-09-10 已确认与当前边界

- 用户要求理解前 agent 成果、判断下一步，并同意先检查/修正输入与评价协议，形成可执行四配置实验规范，再判断训练条件。
- 已完成本次检查和局部修正，见 `protocol_audit.md`、`experiment_protocol.md`。
- 当前正式训练 HOLD，原因是原 CMP 的 GT 依赖、远端独有目标缺失、检测器训练来源与真实消息协议尚未解决；不以硬件/数据资源限制替代研究判断。
- 新因果窗口已生成并验证。尚未完成因果 MTR 接入、四配置模型实验或 LLM 路由训练。

## 最新推进范围

- 用户明确纠正：先推进现在的车车工作，RSU 以后再说。本轮不增加 RSU 或更换到车路数据集。
- 机制仍未最终确定，P/F/I 和序贯查询是讨论中的候选，不能把建议当作已确认完整方法。
- 当前已完成车车单场景恒速接口验证和同信息对照，见 `vehicle_probe.md`；不等于学习型预测、路由有效性或闭环实验完成。

## 本次继续指令与执行状态

- 用户要求“开始下一步”，承接既有车车 P/F 能力验证；未增加 RSU、I 或最终机制确认。
- 已按这一范围完成共享线性残差预测基线的拟合、P_state/P_history/F 对照及两个验证场景评估，见 `learned_capability.md`。该模型未稳定超越恒速，未进入路由训练；不是 MTR 或最终预测器已确定。
- 早期“未训练”描述只适用于对应历史阶段；本轮已实际拟合该小型基线，旧 CMP 四配置正式训练仍未启动。

## 最新纠正：核心能力必须复用已有框架

- 用户明确指出：自行另写的小预测器不满足项目推进要求，至少应使用 CMP 或 V2V-GoT 原框架的模块，或在这些模块上修改。
- 后续以真实原模型、已有权重和必要的数据适配为主线。恒速/岭回归仅作为接口检查或简单对照，其负结果不能充当 ToolV2X 核心能力验证结论。
- 当前优先接入 CMP 的原 MotionTransformer 编码器/解码器及已有单车配置权重，保留因果输入要求；这不等于重新确定 CMP 完整融合架构为最终方法。

## 当前请求：设计完整框架

- 用户提出先搭较完整的框架再看数据是否更科学，随后明确要求“设计一下框架”。
- 本次输出为 [framework_design.md](framework_design.md)：从因果数据、原模型 P/F、消息接收、驾驶任务输出到配对评价的设计草案。
- “V2V-GoT 原驾驶模型 + CMP MTR 能力组件”、P 默认包含可用观测历史、最多两次调用等是本次推荐设计，不能写成用户已确认的最终机制。
- 本轮没有启动该框架的编码、训练或规划推理。后续的研究判断应基于原模型驱动的完整任务链与强固定对照；此前恒速/岭回归诊断不能作为方向否定依据。

## 框架设计后的实施授权

- 用户在阅读设计和纯文本重发后明确“OK，现在开始”，中断后再次要求“继续”。按该框架进入实现，无需重复确认同一设计。
- 实施计划见 `planning_implementation.md`。先接通原 V2V-GoT 任务端和真实 P/F 数据流；最终学习查询机制仍由后续对照决定。

## 最新资源分工与执行状态

- 用户提示 `cmp_repo_notes.md` 有 CMP 调研，可作为接入参考；其中历史资源缺项已用当前盘点更正。
- 用户允许调用 subagent 进行资源盘点/下载，指定 GPT-5.6-Luna Max。
- github.com、githubusercontent.com、githubassets.com、huggingface.co 相关资源由用户自行下载；agent 应提供明确链接、文件和目标路径，不通过子域名、镜像或跳转绕过分工。
- 收到该要求后，先前启动的下载已停止；已找到本地完整 LLaVA/CLIP 历史缓存，链接后离线加载成功，目前没有必须补下的文件。盘点见 `resource_downloads.md`。
- 原模型固定动作任务链已在一个真实验证帧运行，Ego/P/F/PF 均完成 Q8→Q9。实现/验证见 `planning_connection.md`；没有训练新的共享驾驶模型、学习查询器或开展正式规划质量/闭环评价。

## 继续推进证据表示和训练准备

- 用户在上述进展与下一步说明后要求“开始”，按已有授权推进紧凑证据和共享驾驶模型适配训练准备。
- 已完成可还原紧凑格式、原模型真实对照、3027 个训练帧/108 个验证帧的因果输入索引和独立监督标签，以及一个真实训练帧四动作共 8 条监督前向检查，见 `evidence_adaptation.md`。
- 没有优化参数或生成新训练权重。紧凑格式增加目标后仍有 Q9 格式失败，默认 JSON 保留；全量工具证据和正式适配训练仍待完成。
- 下一步沿已批准训练顺序先适配原 CMP MTR，再生成全量工具证据并训练共享 V2V-GoT 任务模型；不因当前弱模型或编码检查结果否定方向，不提前确定查询机制。
