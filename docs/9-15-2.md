最新结果中有一组数据很值得注意：
单独 P：
acquired history = 78
direct history = 68

单独 F：
acquired forecast = 78
direct forecast = 78

而 PF：
acquired history = 78
acquired forecast = 78
direct history = 68
direct forecast = 68

也就是说：
P 出现以后，有 10 组远端实体证据没有作为直接张量进入 planner，而且 PF 中连对应 F forecast 也一起少了 10 组。
这不一定是 bug。
很可能是 P history 使 entity association / ego-role identification 更充分，从而把某些原来 F-only 中无法确认身份的对象过滤掉；也可能来自其他 association/admission 规则。
但正式全量采集之前，我认为必须把这 10 组到底为什么消失查清楚。
否则以后如果：
F > PF

或者：
P/F 对训练贡献不同

你无法判断这是能力本身的问题，还是 receiver 在 P 出现以后改变了 entity admission。
尤其 F-only 的 78/78 与 PF 的 68/78 差异比较系统，不适合直接当普通随机容量截断略过。