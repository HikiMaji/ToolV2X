# SDD ledger — plan: docs/superpowers/plans/2026-09-15-driver-readiness.md

User approved Stage A with 开始. No real training, collection or model generation; no push. Isolated feature/driver-readiness; original main dirty docs preserved. Base sdd/driver-readiness-base.

## Preflight interface scan
| Tasks | Shared interface/file | Resolution |
|---|---|---|
| 1 / 2 | evaluation.framework | Sequential ownership; Task2 adds optional numeric audit after Task1 accounting |
| 1 / 3 | measured cost vs offline validation | Validation does not claim runtime cost; no relabeling prior archives |
| 1 / 4 | accounting version / explicit controls | Actual runs emit accounting identity, preparation stores specification only |
| 2 / 3 | evidence condition / tensor coverage | Receipt-defined conditions for weighting; use Task2 audit for direct coverage, not relabeling condition by usefulness |
| 2 / 4 | audit output | Package requires structural audit, does not invent coverage results |
| 3 / 4 | complete v2 training config | Task3 report supplies exact keys; Task4 validates actual config through same code |
| 1 | cost tests vs compatibility | Historical missing measurements become unknown in derived evaluator; archived bytes unchanged |
| 2 | coverage vs causality | Existing receipt and numeric validator; no second ledger or true usefulness claims |
| 3 | loss version vs resume | Old config preserves old objective; v2 binds objective/weighting/validation and saved progress |
| 4 | freezing vs execution | Metadata only; runtime identity/checkpoint cannot be fabricated before real load |

Ruling: New v2 trains each row's current stage and refinement, with frozen frame-condition weights, retaining old prefix loss under v1 — removes accidental duplicated early-stage supervision while keeping causal generated priors — cost if wrong: objective or weighting needs a new config/version and retraining, no method/protocol change.
Ruling: Conditional bootstrap uses existing initial Ego numeric archives, capped at three epochs before recollecting all four conditions — avoids creating a second collection schema or forged learned trajectories — cost if wrong: bootstrap may still fail acceptance; stop for targeted data/initialization correction rather than silently extend training. No real bootstrap execution authorized in Stage A.

Task 1: pending
Task 2: pending
Task 3: pending
Task 4: pending

Preflight baseline: 34 existing method episode/evaluation tests passed in 4.762s. Task1 active. Task4 metadata prepared independently: 3095 existing physical-train causal rows, 2987 train/108 validation, 8/2 recording groups; source field whitelist9. No arrays/labels/models read; existing 40 training-window exclusions retained.

Task1 RED/GREEN status from implementer: new8 regression checks reproduced None-control zero cost and missing accounting/interrupt records (3FAIL/4ERROR/1PASS initially), first fix8/8GREEN. Relevant old regression in progress; full check_review deferred until final.

Task4 metadata source cross-check: every selected row belongs to existing role scenes and none to excluded scenes; two deterministic endpoint frames per recording yield20 acceptance identities. Preview saved; no labels/outputs used for selection.

Continuation audit: previous Task1 worker no longer live after user continuation; existing uncommitted changes retained. Fresh bounded worker readiness_accounting_finish assigned only to finish legacy checks/report/commit, no restart. Task1 remains incomplete pending review.

Task1 continuation worker confirmed new8 tests GREEN in0.319s; old method_episode/evaluation/controls/budget cases passed within combined targeted run, query_data/value/bundle tail still running. No full suite started.

Task1 validation transport issue: combined targeted process completed but worker lost tool session output, no final exit evidence. Worker had already started bounded split runs only for tail query_data/query_value/bundle_data/control_bundle; control_bundle15PASS0.210s, remaining3 pending. Do not claim combined complete PASS; preserve this limitation and avoid rerunning confirmed modules.

Task1 targeted tail: control_bundle15PASS0.210s, bundle_data12PASS64.633s; query_data22 ran with6ERROR from online state versus saved decision boundary,16 passed. Accounting metadata affects causal archived-prefix reconstruction; authorized minimal query/bundle consumer fix without weakening equality. Query_value still running at last report.

Task1 committed Account for every actual request decision; new8PASS0.266s, failed query6PASS26.840s, representative value1PASS14.192s, bundle12PASS64.633s/control_bundle15PASS0.210s. Root-cause fix in common decision_state keeps policy-visible cost ledger at true prefix while pending decision timing is persisted. Report saved; independent review pending.

Task1 review: one Important — legacy preflight/policy failure with no decision/control record still produces0 rather than unknown; one Minor — unhashable accounting identity can crash aggregation. Fix round1 assigned to continuation worker; no other task started.

Task1 fix round1 committed Handle legacy decision accounting gaps; important historical failed-stage missing timing and malformed identity aggregation addressed,25accounting/evaluationPASS1.089s. Scoped re-review pending.

Task 1: complete (Account for every actual request decision through Handle legacy decision accounting gaps; fix round1 both findings resolved; independent scoped re-review PASS). Task2 starts next; base sdd/readiness-admission-base.

Task2 first RED: missing evaluation/structured.py module assertion fails as expected. Existing structured input/evaluation35PASS baseline. Real numeric archive behavior implementation underway, no models or data read.

Task2 worker first core GREEN:6/6audit testsPASS1.28s, no torch; audit_structured_episode and numeric evaluation structured_audit implemented. Covering prior-only/equivalence/real receipts and masks; final targeted regression/report/review pending.

Task2 committed Audit direct and indirect structured evidence use; targeted41PASS torch-free. Independent task-scoped review active (review_readiness_admission), including acquired-versus-derived accounting semantics. No Task3 implementation started yet.

Task2 independent review confirmed Important: new_remote/previously_acquired_remote currently union receipt-acquired and receiver-derived fields, obscuring acquisition counts in P-local. Minimal fix must split existing ledger sets and test real receipt versus derived forecast; no new ledger. Await complete report before fix dispatch.

Task2 review complete:1Important,noCritical/Minor. Fix round1 delegated to original readiness_admission at sdd/readiness-admission-fix-base; split acquired/derived audit collections, preserve neutral union, targeted RED/GREEN and commit/report.

Task2 fix round1 committed Separate purchased and receiver-derived audit fields; acquired versus derived known/new/previous explicitly separated,23targetedPASS torch-free. Scoped independent re-review active.

Task 2: complete (Audit direct and indirect structured evidence use through Separate purchased and receiver-derived audit fields; fix round1 RESOLVED independent spec/quality PASS). Task3 next, base sdd/readiness-training-base.

Ruling: Extend the existing diagnostic policy IDs with F-current-only and fixed P-current then F-current during A4, reusing the same executor/CLI whitelist — verified current runtime cannot express all four promised collection conditions (only stop, P-current, P-current/F-change exist), so a metadata-only unsupported F recipe would misstate readiness — cost if wrong: replace these diagnostic selectors/config identities before collection; no learned policy, protocol change or real execution this batch.

Task3 implementer initial RED4missing APIs/config confirmed; first19tests18PASS, non-periodic resumed validation caught and minimally fixed. Targeted training+episode green2 active; report defines complete v2 keys. Zero-label rows excluded from v2 optimizer batches, so bootstrap per-epoch interval must bind actual eligible count when independent labels are available in future phase.

Task3 committed Add stage-weighted training and periodic validation; training+episode38PASS; checkpoint-from-earlier-batch future-file copy failure fixed with2targetedPASS. Independent review next. No real resource execution.

Task 3: complete (Add stage-weighted training and periodic validation; independent spec and quality PASS, no findings). Task4 starts at sdd/readiness-preparation-base. Read task-4-preflight.md for verified runtime selector gap and new Ruling.

Task4 active readiness_preparation (sol high), metadata-only package plus approved diagnostic-ID gap. Root updated README/STATUS/github_review with explicitly in-progress StageA notice, not completion. Original main four dirty docs verified byte-identical to pre-work backup.

Task4 worker RED confirms missing package/helper/shared diagnostic IDs, plus unavailable F must STOP. Verified actual diagnostic_conditional_v1 registration, existing public bundle constructor and Task3 strict v2 config; source3095/acceptance20 exact. Package/helper/tests+minimal selector draft now in progress, no resource arrays/model/label read.

Task4 committed Freeze shared driver preparation and acceptance settings;36targetedPASS; exact all3095source whitelist comparison+portable schema+20selection+actualbundleUTF8 budget checks PASS. Request1813B+outer133120B fits196608B.13ownedfiles, prepared_not_executed. Review package points to 3.2MB manifest rather than inlining it; all code/config/metadata diff included. Independent review next.

Task4 review early confirmed2Important: duplicate sample identity with changed g bypasses seen tuple, and validate-only CLI wrongly requires all private preparation files. Minimal fixed identity sets and portable CLI path planned; wait consolidated report before one fix round. No new mechanism/data work.

Task4 full review2Important,noCritical/Minor. Fix round1 original readiness_preparation at sdd/readiness-preparation-fix-base: stable sample and(scene,g) uniqueness, portable validate-only CLI+public docs, bounded RED/GREEN.

Task4 fix1 committed Fix preparation identities and portable validation; explicit sample/local/global uniqueness and zero-private-input validator. RED4expectedfailures, GREEN2focused, final8prepPASS pluscompile/diff0; no36/fullrepeat. Scoped review next.

Task 4: complete (Freeze shared driver preparation and acceptance settings through Fix preparation identities and portable validation; fix1 both findings RESOLVED, independent spec/quality PASS). All4tasks complete; wholebranch review and required full regressions pending.
