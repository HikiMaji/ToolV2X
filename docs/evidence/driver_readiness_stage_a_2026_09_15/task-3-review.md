# Task 3 independent spec and quality review

Spec compliance: PASS.
Code quality: PASS.
No Critical, Important, or Minor finding requiring a change was identified in the packaged Task 3 diff. No implementation change requested.

Reviewed `task-3-brief.md`, `global-constraints.md`, `task-3-preflight.md`, `task-3-report.md`, `review-policy.md`, and the complete `task-3-review.diff`. Applied Ponytail to judge the smallest adequate implementation. Read-only review except this report; no subagents, git mutations, real data/model/provider execution, training, or test reruns.

## Evidence traced

- `src/planning/train_structured_driver.py:167`: old invocation preserves cumulative prefix supervision and exact-repeat behavior. Explicit v2 disables prefix gradient graphs, replaces archived numeric priors with actual detached current-model predictions, and averages only the selected stage plus configured same-evidence refinements. The added literal gradient test distinguishes legacy 11:5:2 from v2 equal stage contributions; direct source inspection confirms the refinement averaging rather than relying only on the equal-loss fixture.
- `src/planning/structured_validation.py:13`: conditions are canonical primitive receipt sets at the selected ledger snapshot; repeated P and receipt ordering cannot create extra conditions. `fit` authenticates rows through `_verify_rows` and `_task_rows` before computing these groups. `frame_condition_weights` splits equal frame weight across represented eligible conditions and duplicate stage rows, normalizes once per recording role, records explicit groups/gaps, and assigns zero weight to rows without labels. `train_structured_driver.py:566` applies those fixed weights before minibatch mean without per-batch renormalization.
- `src/planning/structured_validation.py:60`: full labeled, unlabeled, invalid-output, weighted-invalid and metric-specific denominators remain explicit. Strict `validate_plan` precedes distance metrics. The existing `trajectory_metrics` requires complete horizons for complete-prefix average, permits eligible shorter diagnostics, and cannot drop an invalid prediction from the labeled invalid-rate denominator. Selected-stage and additional refinement outputs have separate summaries and explicit group identities.
- `src/planning/structured_validation.py:111`: selection uses weighted invalid rate, finite complete-prefix distance, then earliest processed batch. No-label/all-invalid/no-complete-prefix cases cannot fabricate an eligible selection. `train_structured_driver.py:531` references the batch checkpoint that the same iteration saves, and preserves old references rather than moving/deleting weights.
- `src/planning/train_structured_driver.py:509`: validation uses frozen validation-role rows and their own recorded weights, no-grad/eval execution, and `finally` restoration of Python/NumPy/Torch/CUDA RNG and model mode. The current network uses zero attention/transformer dropout and no running BatchNorm state. No optimizer update occurs during validation.
- `src/planning/train_structured_driver.py:545`: fresh initial validation, explicit processed-batch cadence, and normal final validation are separate from an interrupted nonperiodic stop. Saved configuration, row semantics, weights, input layout/content, optimizer, RNG, progress, history and selection bind exact resume. Earlier retained checkpoints are copied only up to the resumed batch; history references are checked for presence. Relocation preserves names and semantic input comparison. V1 stays on its old final-validation branch and old config/objective path.
- `tests/test_structured_training.py:403`: actual tiny CPU network/optimizer fixtures cover bundle grouping, strict invalid/partial-label metrics, detached priors, cadence invariance, non-null real saved selection, moved data/run directories, and resume from an earlier checkpoint of a completed run. The small-output constructor still performs actual network forwards.

## Verification and limits

Directly read the recorded terminal logs and exit files: `task-3-green-2.log` / `.exit` show 38 tests passing with exit 0; `task-3-green-resume.log` / `.exit` show both final resume checks passing with exit 0. No passed test was rerun, and no concrete uncovered doubt warranted a new probe.

The shared-interface reads were limited to strict ExecutionSpec plan validation, existing trajectory metric horizon semantics, and the current network's mode-sensitive layers. Root still owns the final whole-branch review and complete regression, including Task 4's generated full config/examples, common observations_only experiment setup, and collection-manifest coverage for completely absent frames. This Task 3 PASS establishes reviewed implementation/contracts and the recorded synthetic CPU checks; it is not a real training, performance, or method-benefit claim. Exact relocation assumes the retained run directory with bound inputs and historical checkpoints, as documented.
