# 现有数据 20 轮训练证据

从已完成的本地运行复制配置、结果、覆盖、逐步记录和核验日志。未包含权重、输入归档或原始数据。

运行目录：`outputs/structured_driver_fit_20ep_2026_09_15_v1/`。16 训练帧、4 个既有开发帧，固定归档证据；不构成独立测试或在线方法收益。`run_training.py` 和 `verify_training.py` 是实际运行脚本快照，依赖本地资源路径，不是可直接移植的数据清单。本次发布没有重新运行训练。
