# 2026-09-12 GitHub 更新与审查范围

本次更新包含中断后的训练保存/恢复修复、最新主线创新候选，以及按用户要求停止训练后完成的第一轮实际轨迹评价。训练保持停止，不自动续训。

代码变化包括数据准备续接、已有准备数据复用、完整检查点原子发布、首次/每 25 步/验证前保存、最近两份周期恢复点保留，以及中断后恢复入口和相应测试。回答位置输出头属于实测未达到提速门槛的可选实现，当前训练没有启用；不宣称已经显著加速。

第一轮模型完成 32 帧 × 五策略的 160 次实际生成。所有回答有效，但各配置总体未超过恒速直行参考，PF/规则的小幅平均改善依赖少量大收益。完整结果及边界见 [评价报告](epoch01_quick_evaluation_2026_09_12.md)。原始模型权重、完整数据、论文全文及重复源码快照不上传。

本次上传评价摘要、逐条指标、配对 CSV、图，以及 `review_predictions.jsonl` 中的原始回答、解析点和离线标签。网页审查环境可仅用 Python 标准库核对 160 条实际回答、ADE/FDE 与各配置均值：

```bash
python outputs/framework_epoch01_quick_eval_2026_09_12/verify_review.py
python scripts/check_review.py
```

第一条不需要模型、GPU 或外部数据。它验证已发布结果的内部一致性，不验证原始位姿标签来源或重放模型推理。原运行 `run.json` 和逐条评价中的绝对路径保留来源含义，不能直接当作网页环境路径；本地 `audit_results.py` 仍依赖未上传的完整生成文件与外部标签。完整重放需要 README 中的本地资源。

发布前本机完整集成 131 项、轻量审查 90 项通过；标准库轨迹复算通过。最终发布检查见本次追加开发日志。

研究文档中的“驾驶方案修订驱动的远端再请求”仍是待验证候选；这批固定/规则条件评价不代表新机制已实现或有效。审查时请分别检查保存恢复正确性、评价口径、现有输入/查询实现与候选设计之间的差距。
