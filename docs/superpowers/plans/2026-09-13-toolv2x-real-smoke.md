# T9 两帧真实联调（用户已授权）

范围：固定已审查的 T9 本地提交与配置；使用冻结 GoT epoch01、CMP full_seed20 epoch09，在两个既有验证录制组各一个首个合法帧运行 Ego、诊断交替、诊断单轮三条路径；核验实际报文、E/derived/Z、原模型轨迹、所有生成/服务成本，保存失败与轨迹图。禁止训练、扩帧、分支枚举或改机制。原图像输入边界：点云生成的 t/t-1 检测与特征图，经原 GoT projector，无真实 RGB。

1. 固定版本：T9 准备代码已提交 main；隔离工作树 feat/t9-real-smoke。模型环境完整回归 345/345（289.657 秒）通过。
2. 执行：冻结输出 outputs/t9_real_smoke_2026_09_13_v1/launch.json；g5526/g7007；保持当前默认几何/排序与 max_targets=4；首次接入采用预先声明的宽松共同 wire cap，全部实际字节仍计费。原始 paired 索引显式投影到合法在线白名单，不增加 GT。
3. 核验：原模型加载一次，各任务真正执行；每阶段保存，失败不删除。单轮 per_rpc 内部两份容量各31744，与普通原语65536不同，故不据此做强单轮公平收益结论。
4. 离线：所有推理完毕后另一个进程才读已存在标签；独立审计员核对结果，输出每帧轨迹图与具体因果账本。停在本次两帧联调，不自动进行后续训练或采集。

协作边界：root 负责配置/执行/结果图；t9_smoke_audit 只读审计，不写源码、不执行模型、不改 Git。两者共享 launch 和终态归档，无源码重叠。实际局部接入缺陷在本批修复并保留原失败；不将普通边界问题升级为全项目暂停。
