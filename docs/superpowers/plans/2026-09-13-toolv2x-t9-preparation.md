# T9 准备批次（已获用户“开始”授权）

范围：发布已完成的 T6 修复/T7/T8 审查快照；补齐方法运行入口、可版本化冻结配置、独立条件单轮监督采集与终态核验。只运行资源无关和 CPU 合成契约测试；不调用真实 GoT/MTR，不采集真实分支，不拟合真实策略，不扩大为全量 T9 效果实验。

约束：沿用 9-12-1/9-12-2 与 T1–T8。P/F 两工具；最多 2 次远端能力；F 完整合法上下文后排序，P 路径不运行 MTR；所有真实费用、失败和物理录制身份保留；不引入新模型/工具/RL。历史产物不覆盖；本批新增实验命令仅在明确调用时运行。

1. 审查快照：更新 README/github_review 的过时 T1/T2 状态，验证实际待提交文件，提交并正常推送 origin/main。密钥只使用本机既有认证，不复用对话里出现过的 token。
2. 运行入口与冻结配置：给 interact 增加显式价值 checkpoint 选择和 bundle continuation 绑定；旧调用保持原诊断行为。新增纯 JSON 冻结规格，保存精确样本集合、录制角色/折、模型/receiver/query/control/value/utility 版本、预算及 checkpoint 快照引用，禁止恢复时使用被替换的价值权重。运行前验证，不能接触 GT 或未购 peer。代码和测试由入口子任务负责。
3. 单轮监督：独立 collect_bundle_branches 与 make_bundle_targets。复用已有 executor/receipt/driver/成本评价，首请求与 continuation 候选在合法可见数据下枚举；STOP 基准是只返回第一次结果后实际完成最终 driver。分支原始终态全部归档，离线标签单独读取，费用从实际外层报文与终态费用重算。记录失败/缺失/不可执行候选；不借用交替产生的 tau1。
4. 合并契约验证、独立复审、文档交接。明确：这批完成的是 T9 实验接入准备；真实链路、分组策略拟合、公平 rollout 与论文效果仍未运行。

实现裁决：为减少侵入，本批 bundle 枚举允许每个候选执行一整个真实单轮 episode，并逐条记录实际运行。相同候选前缀不能伪称已复用；一旦需要第一响应共享以固定监督源，应在真实第一 primitive 返回边界复制 provider 状态并复用该真实包，不能拼接伪造返回。只为采集实现有界前缀复用，不开放线上免费结果读取。
