# 2026-09-11 GitHub 更新范围

用户要求先更新 GitHub。本次提交以当前已验证的代码和已完成结果供网页版审查；实验进行中的状态按发布快照记录。

## 代码与文档

- 上轮网页版审查的五项本机修复，以及对应负例/回归测试。
- 原 CMP MTR 因果监督、原损失训练、预测评价和独立复核；首轮真实适配结果及图。
- full/alternating × 三种子的固定更新预算协议与训练入口。六组未全部结束前不发布冻结结论，当前进度见 `outputs/mtr_stability_v1/progress_at_github_update.json`。
- 共享驾驶端的 direct / Q8→Q9、独立 adapter 目录、失败与指标分母处理，以及原模型可训练 LoRA 加载、一次更新和独立重载检查入口。
- GoT / V2V-LLM × 两种解码的真实单帧 16 次动作记录、失败定位及独立输入/误差核对。

## 精选证据

保留首次 MTR 适配的配置、逐轮指标、前后汇总与完成记录，以及本轮已经完成运行的审计/汇总。驾驶端保存原始回答、逐动作证据和请求/响应 JSON、选择预算、成本及评价记录。它们都来自实际运行，不把接口测试替身作为模型效果证据。

模型权重、完整点云/特征/跟踪窗口、训练缓存、论文全文、下载与编译产物、重复源码快照仍在本机。新运行完整目录在本地 `outputs/`；线上精选文件并不足以脱离外部资源完整重放实验。

## 审查重点

先运行 `python scripts/check_review.py`；有本机完整模型环境时再运行 README 的完整测试。另核实原 MTR 标签与在线输入隔离、相同更新预算和相同验证目标、direct/Q8→Q9 契约、请求/响应成本、只监督回答 token，以及训练与推理加载分支。

常规测试通过不代表原 7B 长序列训练已经成功。目前真实更新检查在长 P 样本 backward 时因注意力内核/显存失败，零 optimizer 更新；已保存失败，并在准备原训练脚本使用的 FlashAttention 2。当前没有正式驾驶适配或方法收益结论。

原始审查笔记 `docs/9-10.md`、`docs/9-10-2.md` 留在本机；面向审查者的处理说明和验证结果在本次提交中。
