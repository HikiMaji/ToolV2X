# 完整基础框架：实现、联调与适配记录

**最新状态（2026-09-12 傍晚）：** 用户要求停止训练后，已完成 [第一轮模型 32 帧 / 160 次实际轨迹诊断](epoch01_quick_evaluation_2026_09_12.md)，不自动续训。所有输出可解析，Ego/P/F/PF/规则 ADE 为 1.741/1.937/1.821/1.722/1.641 米，恒速直行参考 1.533 米。当前尚无稳定协同收益；这批是开发集抽样，不是全量评价或独立测试。完整检查点与最近周期恢复点保留。下文为各阶段历史实现和结果记录。

更新：2026-09-11。本轮已把原 CMP MTR、原 V2V-GoT 驾驶器、实际查询循环和训练/评价接成可执行流程，并完成小样本训练、保存恢复、重载生成。全量 3095 帧五策略准备及训练导出已经完成，正式共享驾驶适配已产生实际参数更新，尚未完成训练或全量生成评价。最终学习查询机制仍是后续研究内容。

## 现在实际运行什么

```text
本车当前/历史特征与状态
  → 本车 MTR 预测和初始证据
  → 选择 STOP / P / F，以及查询区域
  → 实际向邻车服务查询，保存请求、响应和成本
  → 用已返回信息更新证据
  → 再次决策，或因预算/规则停止
  → 构造统一的本车块 + 已查询邻车块
  → 同一个原 GoT 驾驶模型生成未来 3 秒的 6 个点
  → 独立离线评价轨迹误差、失败、调用和成本

独立离线未来位姿 → 训练答案 / 验证答案
已执行的相同输入 → 共享驾驶模型适配 → 保存 → 独立重载生成
```

在线状态不含未来标签，不预读未查询邻车。本车文字块在五种策略间相同；同一个模型接收不同策略实际获得的证据。训练只监督最终答案与 EOS，点云和提示位置不计算损失。

| 策略 | 实际决策过程 | 当前作用 |
| --- | --- | --- |
| Ego | 直接 STOP；没有邻车读取和报文 | 本车独立对照 |
| P | P → 更新 → STOP | 接收当前跟踪状态和历史；本次设置在本车用 MTR 重算 |
| F | F → 更新 → STOP | 使用邻车完整上下文预测后返回查询范围内的目标 |
| PF | P → 更新 → F → 更新 → STOP | 固定两次调用对照 |
| rule | 依据本车速度先 STOP 或 P；再根据实际 P 返回决定 F 或 STOP | 验证结果能够影响后续调用的规则基线 |

规则具体定义和接口在 [实现规格](framework_implementation_v1.md)。单次任务最多 2 次查询；纯观测 P 接收也可执行，但本次统一训练固定使用 `local_mtr`，未展开另一套训练。P/F 的提供方是同机隔离服务，报文实际序列化；没有实车网络测量。

固定 P/PF 的完整 P 可在本地用同一 MTR 得到与 F 相同的邻车预测。这是同信息计算位置对照；规则的 ROI-P 可能缺少 F 在提供方使用的完整上下文。本轮没有联合重跑 Ego+P 的融合预测器，未把“收到 P”限定为必须预测的通用设计。

## 已完成的真实联调

物理录制组按既有 8 个训练组、2 个开发验证组隔离，每组确定性选取 2 帧，共 20 帧。每帧执行 Ego/P/F/PF/rule 五种策略，得到 100 个完整任务；80 条训练样本来自 16 帧，20 条验证输入来自 4 帧。第一次误按 scene 片段抽样的 34 帧尝试保留在 `framework_debug_v1`，修正后的结果是 `framework_debug_v2`。

1. 冻结 CMP MTR 实际执行，100 个任务均完成，Ego 的邻车访问为零。规则在 20 帧中出现 17 次 P→F、1 次 P 后停止、2 次直接停止；并非所有条件都运行固定 PF。
2. 原 GoT 完成总计 10 次优化器更新，累计消费全部 80 条训练行，完成 1 轮和全部 20 条验证损失计算。使用 AdamW、LoRA/projector 学习率 2e-5、有效批量 8。按录制组与策略均衡的验证损失为 0.686655。
3. 保存后继续训练，逐项核验 LoRA、projector、优化器和 CPU/CUDA/Python/NumPy 随机状态恢复一致。独立从原模型连续训练的运行在第一次反向就有细微数值差异，因此不宣称 GPU 跨运行逐位确定性。
4. 从保存的一轮检查点重新加载模型，对预选的全部 4 个验证帧执行五策略生成：20 次均生成可解析的六点轨迹，随后完成离线评价。
5. 独立复算全部 20 条轨迹的 ADE/FDE，核对 100 个任务的实际报文字节、物理划分、容量和本车输入一致性，均通过。完整测试 115 项、轻量测试 87 项通过；独立审查发现的两项 P1 已修复并复审关闭。

小样本训练输出明确记录 `formal_adaptation_complete=false`。10 次更新的作用是验证完整训练流程可用，并不代表共享驾驶器已适配充分。

## 轨迹质量仍需正式适配

下面保留小样本联调的全部结果，防止把“可解析”误写成“驾驶质量合格”。每种策略只有 4 帧；没有用这些数值选策略、选初始化或判断研究方向。

| 策略 | 可解析 / 尝试 | ADE3（米） | FDE3（米） | 平均调用数 | 平均响应字节 |
| --- | ---: | ---: | ---: | ---: | ---: |
| Ego | 4 / 4 | 13.014 | 23.574 | 0 | 0 |
| P | 4 / 4 | 15.520 | 20.214 | 1 | 41711 |
| F | 4 / 4 | 12.639 | 21.479 | 1 | 36706 |
| PF | 4 / 4 | 13.086 | 22.625 | 2 | 78417 |
| rule | 4 / 4 | 29.451 | 34.623 | 2 | 18018 |

当前纵向速度、零转向外推的同帧 ADE3 为 1.175 米。联调模型有明显错误，rule 在 g7007 还生成了六个相同的远距离点。评价保留这些输出和运动学指标；“解析失败为零”不表示物理可行性或任务质量达标。原始二维轨迹全部保存在生成文件，下面展示所有四帧的前向位移随时间变化，没有挑选案例。

![全部四帧的联调轨迹](figures/framework_integration_trajectories.png)

输入容量也是现存限制：这 20 帧平均保留本车 2.65 条记录，P/F/PF/rule 分别保留邻车 1.35/2.80/2.30/1.80 条。PF 记录包含来源和模式，可能有同目标重复表达；它不是独立目标数。完整覆盖率和来源记录均保存，不能把当前文字选择称为已优化的融合器。

## 两项审查修复

- 工具任务先保存身份与请求，响应及已发生成本在接收处理之前持久化。已用故障注入确认：P 已返回后本地 MTR 异常，或第二次服务异常，历史账本仍保留。无法确定的部分成本显式标为不完整，失败任务保留在尝试集合内。
- 准备阶段核对每个预选帧与策略的完整集合。生成前保存任务清单，评价要求完成标志、任务数量和身份均一致；缺少生成记录时拒绝发布完整结果。训练导出遇到失败任务会停止，不静默删掉失败条件。

成本包含请求/响应字节、提供方计算、接收处理及驾驶生成。报告中的计算耗时是已测时段之和，本车共享计算按每个对照任务计入一次；不包含模型装载、真实网络传输和原感知预处理，不能当作车辆部署时延。

## 全量单配置运行与中断续接

运行目录：[framework_baseline_v1](../outputs/framework_baseline_v1/)。2026-09-11 19:03（UTC+8）已启动独立进程，执行的是该目录 `code/` 中保存的代码；后续工作区编辑不会改变这次运行。阶段顺序为：

```text
3095 帧 × 5 策略的真实任务
  → 独立导出 14935 条训练行和 540 条验证输入
  → 原 GoT 初始化，最多 3 轮共享适配
  → 按录制组/策略均衡的验证答案损失选检查点
  → 全部 108 个开发验证帧 × 5 策略实际生成
  → 统一离线评价
```

训练不使用小样本联调权重。配置固定为 AdamW、LoRA/projector 学习率均 2e-5、有效批量 8、seed 20、梯度裁剪 1、最多 3 轮、patience 2，MTR 冻结。此处的 3095 帧是既有清理后的研究因果索引，包含 2987 个训练帧与 108 个开发验证帧；不是整个原始数据集的全流程复现。

GitHub 提供 [发布时进度快照](../outputs/framework_baseline_v1/progress_at_github_update.json)，它不是实时状态。之后核查发现原两进程均已退出，最后完整进度 1718 帧，旧状态仍写 running，原因未确定；正常异常处理之外的进程退出不保证产生错误文件。因此不能只读 JSON 判断进程正在运行。

2026-09-11 23:04（UTC+8）在 `outputs/framework_baseline_resume_v1/` 启动同配置续接，复用经核对的 8590 个完整任务，重跑中断帧，原产物不变；完整 118 项与轻量 90 项检查通过。当前实际阶段读取新目录 `status.json`、`episodes/progress.json` 并核对进程，细节见 [反馈回复](review_9_11_2_response.md)。本文没有把后续正式训练、生成或评价写成完成。

下一次工作先读取该运行状态与产物，继续这一套配置。完成后先判断共享驾驶器是否学会基本任务、是否仍有重复点或明显运动错误，再处理证据覆盖和查询机制。当前规则是功能基线，尚无学习查询策略、RSU/I 或闭环驾驶结果；本轮不提供方法收益或论文可录用结论。

## 全量准备完成与正式训练启动

2026-09-11 23:48（UTC+8），续接准备正常退出，完整 **3095 帧、15475 个任务，任务失败 0**。其中复用旧完整前缀 1718 帧，新执行 1377 帧；原部分尝试仍保留在原目录。23:50 训练导出正常退出，生成 **14935 条训练行、540 条不带答案的验证输入和 540 条独立验证损失行**。研究划分为 2987 个训练帧、108 个开发验证帧，分别覆盖八个和两个录制组，每帧都有五种策略。

独立运行核查逐行确认：完整任务集合和身份、原始准备提示、同帧共用本车证据/特征路径、训练与验证录制组不重叠、标签对应、验证输入没有答案字段，以及 40 个已停用断点窗口没有重新进入数据。核查只读保存产物，不运行模型、不重算原始特征，也不把既有预训练来源问题标成已解决。结果见 `outputs/framework_full_audit_v1/export_verification.json`。核对脚本已在真实联调数据及三种临时故障副本上验证，原始数据未修改。

对全部任务补齐了前轮要求的重复统计，分母为任务行数；“额外重复行”指每帧五行超过唯一输入数的部分：

| 范围 | 总行数 | 完整提示额外重复 | 去掉查询元数据后的内容重合 |
| --- | ---: | ---: | ---: |
| 训练 | 14935 | 297，1.99% | 1087，7.28% |
| 开发验证 | 540 | 0 | 92，17.04% |
| 合计 | 15475 | 297，1.92% | 1179，7.62% |

保留所有可见字段的规范化结果与完整提示统计相同。全部完整重复均为 rule 在静止场景直接停止时的 Ego=rule，未发现 rule=PF 或 rule=P 的完整提示重复。按录制组分别为 `2022-03-17-10-43-13` 的 219 帧、`12-14-33` 的 20 帧、`14-44-47` 的 58 帧；总体比例低不代表这种集中分布没有影响。

规则全量路线为 2669 次 P→F→STOP、129 次 P→STOP、297 次直接 STOP；这些直接 STOP 都在训练侧，当前开发验证不覆盖这一类规则场景。内容重合统计删除了实际可见的 ROI、查询/字节/状态等信息，不能直接当完整输入去重键，但应与截断一起用于后续解释。五策略原训练权重保持不变，没有在这一轮加入唯一状态消融或 Ego-max 训练。

正式训练于 23:50 进入训练入口，沿用原 GoT 初始化与已有单配置，`integration_only=false`。23:54 核对时，管理 PID 3013 与训练子进程 PID 4824 均存在，已执行 **13 次优化器更新、104 条样本**，已观察损失和梯度范数均有限；运行的 42 个 src Python 文件与工作区对应文件逐字节一致。记录在 `outputs/framework_full_audit_v1/training_start_verification.json`，这是启动核对快照，不是实时进度。

这次只更新共享驾驶器的 LoRA/projector，MTR 冻结，查询规则不学习。当前还没有完成一轮全量训练，也没有本轮全量生成或质量结果；不能从初期训练损失推断协同收益。后续读取 `outputs/framework_baseline_resume_v1/status.json`、`training/progress.json`、`training/selection.json`（保存后才存在），并核对实际进程。准备与导出已经完成，无需再次生成或重启同配置。

## 2026-09-12：训练效率检查与待执行的真实测速

**后续决定：01:16 用户取消本次提效，等待进程已退出，下面的待执行测速/续接计划不再执行。原训练保持运行，候选优化未启用。取消证据在 `outputs/framework_efficiency_handoff_v1/cancellation.json`；下文保留 01:04 时的准备与检查记录。**

用户授权尽量利用 RTX 4090 提高当前训练效率。01:04（UTC+8）观察原训练仍在第一轮，346 步/2768 条样本；最近 100 步平均 **12.59 秒/有效批量 8**。已经使用 BF16 与 FlashAttention 2，GPU 约 100% 利用率、显存 23600/24564 MiB。训练是原 7B/LoRA 的实际适配；不能仅靠增加批量假定能够提速，也不能把三轮初始预算说成已证明必需的最短时长。

本次实现可选回答位置输出头：全部上下文继续进入原 Transformer，只在最终词表投影前选出下一 token 有监督的位置。实际首帧五策略各有 63 个受监督 token，总序列长 1969–3668；因此该部分不再为所有忽略标签位置构造词表 logits。**这只减少输出头计算和相关显存，不能把位置减少比例当整体训练加速倍数。** 原参数、输入、有效批量、逐样本平均、因果移位、EOS 监督、AdamW 和原验证选模路径均保持。

小型原 Llama + PEFT/LoRA、dropout、梯度检查点的 CPU 检查中，完整与选择输出路径的损失、全部可训练梯度和输入梯度一致；不连续标签掩码及无有效监督也有覆盖。完整回归 124 项、交接故障/退出测试 5 项通过，独立复审通过。审查发现并修复了数值门槛可被噪声无限放宽、selection 落后时可能使用旧轮次、原进程刚退出导致交接误报失败的问题。

当前程序只在每轮结束保存完整状态，原第一轮尚未保存，因此本次没有直接终止重跑。已于 01:04 启动 `outputs/framework_efficiency_handoff_v1/handoff.py`（初始 PID 7895），等待第一轮完整保存后执行：

1. 核对进程身份、确认暂停成功，核对检查点、优化器、随机状态、参数文件可读，以及没有检查点之后新记录的完整更新；失败则恢复原训练。原检查点后正在执行的工作可能重放。
2. 在新目录 `outputs/framework_baseline_efficiency_v1/` 重新精确加载原状态，用相同真实有效批量交错比较两条前后向路径：各一次热身、三次计时，不做优化器更新。固定 loss 绝对误差上限 0.001、整体梯度相对 L2 0.01、单参数张量相对 L2 0.05；原路径自身重复也须稳定。
3. 数值通过且中位前后向时间至少缩短 3% 才启用；基准失败或未达门槛则保留原路径。从检查点再次完整恢复后继续余下训练、原验证选模、540 次生成及评价。测速不计入训练步数，不更换数据、MTR 或模型。

`framework_efficiency_handoff_v1` 保存冻结源码、启动前资源核对、原速度和 token 观察、测试日志及交接状态。相对活动训练的 `src/vendor`，仅 `src/planning/train_driver.py` 有实现差异；模型源码、其余输入模块与资源一致。新运行的 `benchmark/performance.json` 会记录真实速度/梯度比较，`efficiency_decision.json` 记录实际是否启用。

**截至本次记录，等待进程已启动，原训练仍继续，尚没有真实 7B 新路径测速、续训或整体加速结果。** 当前是经检查的候选优化与自动执行安排，不是已完成提速，更不是协同驾驶质量结论。

## 入口与产物

| 用途 | 位置 |
| --- | --- |
| 决策、真实查询和停止 | `src/planning/episode.py` |
| 统一驾驶输入 | `src/planning/context.py` |
| 真实任务 / 独立生成 | `src/planning/run_framework.py` |
| 独立监督导出 | `scripts/prepare_framework_training.py` |
| 多样本训练、验证和恢复 | `src/planning/train_driver.py` |
| 完整任务评价 | `src/evaluation/framework.py` |
| 自动顺序运行入口 | `scripts/run_framework_baseline.py` |
| 100 个真实联调任务 | `outputs/framework_debug_v2/` |
| 一轮训练 / 恢复逐项核验 | `outputs/framework_training_epoch_v1/` |
| 20 次原始生成 / 评价 | `outputs/framework_generation_v1/`、`outputs/framework_evaluation_v1/` |
| 独立复算、测试和启动日志 | `outputs/framework_integration_v1/` |

完整基线的可重放命令（新运行须使用新输出目录；当前任务已启动，无需再次执行）：

```bash
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 \
  /root/autodl-tmp/conda-envs/llava/bin/python scripts/run_framework_baseline.py \
  outputs/framework_baseline_v2
```

依赖本机现有外部权重、GoT 处理后数据和因果窗口。没有补下原始 V2V4Real，也没有重跑完整 CMP 感知或原 GoT 全任务训练。实际驾驶输入仍是原点云检测特征与文本，没有真实 RGB；直接六点任务没有执行 Q8 或使用 Q8 标签作为父回答。

GitHub 仅上传这次联调的精选文本：全部 20 个验证任务和生成，以及 2 个训练帧的规则提前停止任务。100 项任务索引保留原样，索引中的其他任务、特征及源码快照需要原工作站资源；该精选归档不能直接当作完整输入目录执行。发布检查通过仓库相对位置读取这些原始文本，不重写其历史绝对路径。具体范围见 [更新说明](github_update_framework_2026_09_11.md)。

## 2026-09-12 上午核查：第一轮中断，缺少可恢复检查点

11:09 核对原管理/训练进程均已不存在，GPU 0%/4 MiB。最后训练日志更新在 02:55:07（UTC+8），875 次连续更新、7000 条训练行，第一轮 46.9%。全部已记录 loss 和梯度范数有限；首 100 步与末 100 步平均训练损失为 0.3806 和 0.1537，不同批次统计只能说明训练中损失下降，不能代替验证或驾驶质量。没有 selection、result、failure 或轮末检查点，未完成任何本轮验证/生成。

原训练只在完整轮末或显式 integration max_steps 触发时保存；本轮 max_steps 未设置，因此这些更新尚未落盘，loaded_adapter 中的权重仅为原 GoT 初始化的链接。这使得已执行的约三小时训练无法从最近进度恢复，是必须补上的保存机制缺口。已完成的 3095 帧准备及训练/验证导出目录仍在，可以复用。

当前环境 PID 1 约于 11:02 启动，支持运行环境曾重启的判断，但不能据此确定最后更新后的准确退出时间或触发原因。日志没有异常栈，dmesg 不可访问；当前 cgroup OOM 计数为 0，不能排除旧容器实例中的事件。01:16 取消提效时原训练正常，并继续更新到第 875 步；优化交接未执行，取消记录保持原样。

证据在 `outputs/framework_training_interruption_2026_09_12/`，原状态/进度日志不覆盖。下一步应先加入周期性完整状态保存及中断恢复，再复用原数据从原初始化重跑；保持既有模型、数据与训练配置，已取消的提效不恢复。本次只是状态/退出线索核查，没有修改模型训练实现或启动重跑。

## 2026-09-12：已重启并验证完整恢复与周期保存

用户在发现中断后重新要求加速重跑。新运行 `outputs/framework_baseline_restart_v1` 复用原数据，从原 GoT 初始化重新开始；旧未保存的 875 步不计入。三种低风险性能候选均未得到足够收益，原 BF16/FlashAttention 2 配置保持。已修复首步、每 25 步和验证前的完整状态保存、原子发布和恢复入口，完整 131 项检查及独立代码复审通过。

真实首步保存后退出并重载，参数、AdamW 与四类 RNG 全部一致，后续步骤正常增长。第 25 步周期检查点实际发布并经独立读取验证；首次与周期写盘约 5.37/4.71 秒，当前更新约 12.4 秒/步，没有显著加速结论。详细基准、保护机制、路径和当前状态见 [重跑记录](training_restart_2026_09_12.md)。此次完成的是训练重启和进度保护验证，尚无新一轮验证或驾驶质量结果。
