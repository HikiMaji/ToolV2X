当前先不要扩大真实分支采集、不要训练 T8 value policy、不要恢复 GoT/MTR 长训练，也不要立即修改 receiver 排序规则。

请基于当前 main 做下面两部分工作，完成后停止并汇报。

### A. 先修两个训练监督入口的不变量

#### A1. one-shot / bundle 训练必须再次验证原始 research role

当前 alternating 的 `training_examples()` 会明确拒绝 validation recording 进入训练，但 bundle/one-shot 路径主要依赖 `train_recordings` 和 fold，缺少同等级的原始 role 校验。

要求：

1. 从 bundle branch archive 的原始 `selected_index.jsonl` / 原始归档恢复并验证：
   - `sample_id`
   - physical recording
   - role
   - fold
2. 任何进入 `fit_bundle_continuation()` 的训练 recording 必须在原始在线索引中为 `role=train`。
3. validation recording 即使被错误写进 `train_recordings`，也必须在 normalization / optimizer update 前拒绝。
4. 不改变现有 train/validation 划分。
5. 加 RED→GREEN 负例：
   - 合法 validation bundle archive + 故意加入 `train_recordings` → 必须拒绝；
   - 合法 train archive → 正常通过。

#### A2. alternating value training 重新绑定质量损失到原始 trajectory + label

当前 T7 制作 `source_loss / terminal_loss / target` 的公式本身保留，不修改。

但 T8 加载 target 时不能只核对：

`target == source_loss - terminal_loss - cost_penalty`

还需要像 measured bundle targets 一样重新从原始数据验证质量值。

要求：

1. 训练加载 alternating targets 时重新读取：
   - source prefix 的真实最后方案；
   - terminal branch 的真实最终方案；
   - 对应的独立 offline label；
   - frozen utility_spec。
2. 重新计算 source ADE/FDE、terminal ADE/FDE、source_loss、terminal_loss 和最终 target。
3. 原 target 表若被同时修改 `terminal_loss` 和 `target`，即使代数仍成立，也必须拒绝。
4. 不重新运行 GoT/MTR，只做离线原始产物复算。
5. 保持失败终态、STOP=0 和现有 incremental cost 定义不变。
6. 增加 RED→GREEN 污染测试。

不要为此引入密码学认证、复杂数据库或新模型。

---

### B. 用现有 T9 两帧做 receiver admission 审计，不要先改算法

对象只用已经保存的：

- g5526
- g7007
- Ego / alternating / one_shot 已有归档

不要重新调用模型。

新增一个只读诊断脚本/报告，完整追踪：

`provider ranking → response records → ledger units → receiver ordering → token growth → admitted Z → dropped fields`

对每一个 remote unit 至少输出：

- frame
- arm
- request stage
- tool / mode
- provider rank
- track_handle
- field kind
- context scope
- anchor distance
- acquisition origin：remote / receiver-derived
- 单独加入该 unit 后 prompt token 增量
- 累计 prompt tokens
- admitted / dropped
- dropped reason
- 最终 GoT object index / prompt section（若 admitted）

重点回答以下问题：

1. g5526：
   - alternating `F(change)` 的 `[6,20,3,8]`
   - one-shot `F(current)` 的 `[3,6,20,4]`
   为什么最终都只剩 target 20？

2. g7007：
   - alternating `[0,21,5,11]`
   - one-shot `[0,5,2,21]`
   为什么最终都只剩 target 0？

3. 对每帧量化：
   - `remote_units_total`
   - `remote_units_retained`
   - 不同 target 覆盖数
   - 同 target 占用几个 unit
   - history / receiver-subset forecast / provider-full forecast 各自 token 成本
   - 若禁止同一 target 连续占多个 unit，在不改变总 context budget 的情况下理论上能额外容纳多少不同 target

这里最后一项只做 counterfactual admission simulation，不调用 GoT，不生成新的驾驶结果。

---

### C. 暂时不要做的事情

本批不要：

- 不要直接把 receiver 改成 change-rank 优先；
- 不要只给 alternating 特殊优先权；
- 不要删除 receiver-subset 或 provider-full forecast 并声称它们重复；
- 不要增加新的 fusion network / tool encoder；
- 不要扩大 context window；
- 不要采集更多真实帧；
- 不要训练 value policy；
- 不要重新训练 GoT / MTR；
- 不要修改旧 T9 结果。

如果发现 receiver admission 确实是主要瓶颈，可以在报告最后提出 **2–3 个所有方法共同使用的 receiver v2 候选设计**，但先不要实现。

候选可以考虑：
- target diversity / round-robin；
- 每 target field 配额；
- task relevance + token cost 的统一 admission score；

但必须满足：
- Ego/P/F/PF/one-shot/alternating 共用同一规则；
- 不读 GT；
- 不读未购买 peer 信息；
- 固定 context / generation budget；
- admission 本身不能偷偷使用 alternating 独有的 τ1 信息去偏袒主方法。

### 完成后汇报

请给出：

1. A1/A2 是否成功复现问题；
2. 修改文件；
3. RED→GREEN 测试；
4. 完整轻量回归结果；
5. 两帧 admission 表；
6. 每个 unit 的 token 成本分解；
7. 为什么不同 provider ranking 最终塌成相同 Z；
8. 2–3 个 receiver v2 候选方案及各自公平性风险；
9. 明确停止，不进入新训练/新采集。