# 数值驾驶v2：现有四条件数据的一轮真实适配

日期：2026-09-15。用户明确要求开始训练。本批使用刚准备的[降低保存频率配置](../configs/structured_driver_training_next_v1/training_v2.json)，执行现有20帧的一轮小批训练；没有扩至3095帧或20轮。

## 执行范围与实际完成

- 复用原80个真实Ego/P/F/PF任务，160条完整阶段样本；128条训练来自16帧/8个录制组，32条开发验证来自4帧/2个录制组。物理数据split仍为train，开发组不是独立论文测试。
- 新目录从seed7 v2初始化训练，初始参数与此前验收未训练权重逐项一致；未使用旧v1或本次4步检查点冒充warm start。
- 已完成1轮/64次AdamW更新；实际优化样本为64 Ego、32 P、16 F、16 PF，按帧/条件权重平衡，未把重复Ego阶段作为独立新帧。所有128个训练阶段按冻结样本多重集核对一致，验证样本没有参与更新。
- lr1e-4、weight_decay0.01、batch2、refinement1、完整driver/ExecutionSpec不变。保存和验证间隔500批；本轮不足500批，因此只保存初始0步、结束64步的完整检查点，并在两个时点验证。两份检查点及所选模型均独立重载通过。
- 本轮331.3秒（含完整归档复核、快照、验证和保存）；PyTorch峰值allocated约381.1 MiB，reserved约416.0 MiB，不等于整卡上下文占用。

## 开发验证结果

下表是4个已使用开发帧的所选阶段、got_prefix_L2_avg（米）。公式沿用共同评价器，但样本、任务、输入不等于GoT/CMP论文设置。

| 条件 | 初始化 | 第64步 | 第64步合法阶段行 |
|---|---:|---:|---:|
| Ego | 0.397703 | 0.401119 | 16/16 |
| P | 0.397205 | 0.396930 | 8/8 |
| F | 0.397038 | 0.397463 | 4/4 |
| PF | 0.396728 | 0.393571 | 4/4 |

共同选模实际选择 `checkpoint_000000.pt`（第0批）。完整所选阶段与额外refinement指标、有效/无效分母保存在[机器结果](evidence/structured_driver_small_fit_2026_09_15/summary.json)，不只报告较好的条件。

这次整轮结果只能说明该小样本上的真实适配表现。四条件共用权重并不自动消除生成次数、信息量和既有开发集使用的差异；不能据此宣称序贯请求收益、超过原论文或闭环安全。

## 监督与实际计算边界

P/F内容来自此前真实服务归档。训练时以当前模型重新产生前缀方案并detach，再监督所选阶段及一次同证据修订；没有GT prior。证据检索与排序仍固定，不会随训练模型的新轨迹重新调用provider。MTR和GoT本批调用次数均为0，训练更新的是数值驾驶器。

全部实际更新的loss、梯度与更新后参数有限，105个参数张量改变；训练过程中累计无效prior屏蔽0次，原因原样保留于机器结果，不能把屏蔽后的离线训练当在线失败已解决。

公开加载器对第64步检查点重新计算全部32条开发行及refinement。与训练内记录的指标精确相等：True；数值最大差0。有效/无效计数一致；本次未新增独立训练支线，亦未宣称CUDA逐位续跑。

## 文件、检查与停止点

生产代码没有修改。新增本机执行目录、完整配置/数据引用快照、训练trace、0/64步权重、独立验证及本文/精选证据/状态入口。57个生产源码文件逐字节未变；80个原任务完整内容与训练快照一致。

本批未重跑无改动的完整单元套件；完成依据为真实64次更新、全部条件覆盖、检查点完整重载、独立开发复算和原输入一致性检查。没有新训练数据采集、压缩reader改造、策略学习、额外训练轮次、权重删除或GitHub推送。

原始结果：`outputs/structured_driver_fit_2026_09_15_v1/`；完整证据见[索引](evidence/structured_driver_small_fit_2026_09_15/README.md)。已按1轮上限停止；是否扩大样本或调整训练需结合本结果判断，不自动继续。
