# 2026-09-11 完整框架 GitHub 更新

本次更新将真实 P/F 决策循环、统一驾驶输入、共享模型训练/恢复和完整任务评价发布到已有仓库。原 GoT 与 CMP MTR 的接入继续复用既有源码和权重接口；最终学习查询机制尚未确定。

## 上传范围

- 完整运行框架及对应测试；按物理录制组准备数据、固定本车块和独立邻车块、P 纯观测/本地重算选项、实际规则与固定策略、失败账本、全任务评价、单配置顺序运行入口。
- 本轮之前尚未发布的 64 帧证据核查、3095 帧 Ego/F 配对准备代码、报告和汇总；保留各自发生时间与完成边界。
- 完整框架设计对照、实现规格、真实联调报告和全部四个验证帧图；README、STATUS 和审查说明同步更新。
- 真实联调的全部 20 个验证任务和原始生成、对应的 4 条离线验证标签、2 个训练帧规则提前停止实例，以及评价、训练/恢复记录、独立复算摘要。
- 全量后台任务的配置和发布时进度快照。快照带有采集时间，不代表后续训练或评价已经完成。

权重、优化器二进制、完整训练行、点云/特征/历史窗口、论文全文、下载编译文件及重复代码快照留在本机。原始审查草稿 `docs/9-10.md`、`docs/9-10-2.md`、`docs/9-11.md` 继续保留在本地；审查处理说明已发布。

## 结果与解释

20 帧五策略共 100 个真实任务已完成。共享驾驶器在其中 16 帧的 80 条训练行上完成一轮 10 次更新，保存/恢复后核对 LoRA、projector、优化器和随机状态一致。对 4 个开发验证帧重载生成共 20 次，均可解析，但包含一条六点重合输出；轨迹误差和简单运动参照见 [联调报告](framework_baseline_results.md)。这属于流程联调，未完成正式共享驾驶适配或方法收益验证。

100 项任务索引保留原样，发布的是其中全部 20 个验证任务，加上 g2932 和 g5465 的规则任务。该归档可以审查真实提示、报文与生成，但没有完整资源，不能直接作为 `prepare` / `generate` 的完整输入目录重放。历史绝对路径没有改写成虚构的可移植资源路径。

全量任务继续使用既有清理索引：2987 个训练帧和 108 个开发验证帧，五种策略共 15475 个任务，预计导出 14935 条训练行和 540 条验证输入。按固定单配置最多训练三轮，选验证答案损失检查点，再生成和评价。当前实际阶段以 [发布快照](../outputs/framework_baseline_v1/progress_at_github_update.json) 为准。

## 复核与审查

资源独立入口为 `python scripts/check_review.py`；两个新增真实归档检查重建全部 20 条提示、独立重算轨迹误差、核对 22 个任务的实际报文字节和提前停止，并要求退化输出继续保留。本机完整测试依赖原模型环境和外部资源，不能用轻量检查替代。

发布验证记录保存在 `outputs/framework_review_2026_09_11/`：本机完整 117 项、轻量 89 项通过；从 Git 暂存内容导出的独立副本，在只有 NumPy/SciPy、没有 PyTorch/Transformers 且外部资源路径不可用的环境中，89 项轻量检查通过。新增发布内容约 8 MiB，未包含权重、完整数据二进制或凭据。后台进程运行固定代码副本，本次提交与上传不改动它正在执行的代码。

网页版审查可直接使用 [审查入口](github_review.md) 中的任务描述。优先追踪查询前信息隔离、训练/推理提示一致性、真实反馈决定下一步、失败成本与完整分母、恢复状态以及录制级划分；不要把功能规则、格式通过或这次短训写成最终学习调度和方法效果。
