# 数值驾驶 v2：现有数据 20 轮训练

## 完成结果（发布时更新）

运行状态为 `completed_verified`，已完成 20 轮/1280 次更新并停止。最终开发指标独立重载复算完全一致；选模仍为第 0 步。

| 更新步数 | 开发 selected-stage got_prefix_L2_avg（米） |
|---|---|
| 0 | 0.397169 |
| 500 | 0.743398 |
| 1000 | 0.955165 |
| 1280 | 0.528012 |

这是固定证据、既有小样本开发集的结果，不是与 GoT/CMP 论文同协议的正式比较，也不证明协作机制收益。用户已将退化原因排查后置；当前推进导航来源与接口研究。公开[结果证据](evidence/structured_driver_20ep_fit_2026_09_15/README.md)。以下保留启动时的配置说明。

2026-09-15，用户要求开始训练。启动前提供规模选项；未收到进一步选择时，按已说明的现有数据 20 轮方案执行。本记录是启动快照，完成状态以运行目录 status.json 和 summary.json 为准。

- 数据：沿用上一轮真实 80 个 Ego/P/F/PF 任务，128 条训练阶段行来自 16 帧/8 个录制组，32 条开发验证行来自 4 帧/2 个录制组；并非全量或独立测试。
- 配置：相同 v2 seed7、AdamW lr1e-4、batch2、refinement1；只把上一轮配置 epochs 从 1 改为 20，共 1280 次更新。保存与验证仍每 500 批及初始/终点，预期 0/500/1000/1280 四份完整检查点。
- 初始化：从 seed7 重新初始化。现有精确恢复接口禁止更改配置，不能把改轮数称作上一轮的精确续跑；旧权重保留。
- 数值驾驶器是唯一更新对象；P/F 为原真实归档，当前模型重新计算前缀。没有新 GoT/MTR 调用、provider 采集、全量扩帧或查询策略训练。
- 完成后监督脚本自动核对 1280 次真实更新、20 轮训练行覆盖、完整检查点加载和最终开发指标独立复算；失败保留日志，停止后续流程。
- 本轮只判断小样本学习趋势，不能据此宣称协作/序贯机制有效或优于 GoT/CMP。

运行目录：`outputs/structured_driver_fit_20ep_2026_09_15_v1/`。

日志：`training.log`；实际逐步记录：`training_trace.jsonl`；监督状态：`status.json`；完整配置：`training_config.json`；核验：`verify.log` 和 `summary.json`（仅在核验成功后生成）。此次未修改生产训练代码，未推送 GitHub。
