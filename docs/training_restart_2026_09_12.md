# 2026-09-12：训练重跑、保存修复与加速实测

用户在确认原训练中断、875 步参数更新没有保存后要求加速重跑。新执行目录是 `outputs/framework_baseline_restart_v1/`，准备数据复用 `outputs/framework_baseline_resume_v1/episodes` 和 `training_data`。原中断数据与日志保留；未保存的 875 步不能恢复，也不计入新训练。

## 保存与恢复修复

第一步更新后立即保存，此后每 25 步保存，完整一轮的训练行结束、验证开始前也保存。完整状态包括 FP32 LoRA/projector、AdamW 一阶/二阶矩、CPU/CUDA/Python/NumPy RNG、轮次、下一行、累计更新数和验证选择历史。每个检查点先写入暂存目录，将文件和目录同步后原子发布；部分写入不会被当作完整检查点。

只轮换本次新运行生成的周期恢复点，保留最近两份；轮末选模检查点和此前产物保留。按当前约 12.6 秒/步估算，25 步约五分钟，另有保存耗时；这是周期配置，不承诺外部环境异常下最多损失恰好五分钟。

恢复入口比较完整的周期/轮末检查点，忽略未完成暂存，兼容首步检查点目录与重复中断后的来源追溯；拒绝循环引用。验证前恢复点重新做验证，不重复已完成更新。训练已达预算而生成/评价未完成时直接续接后续阶段。初次重跑先运行正式第 1 步、保存、退出，再从该状态重载并继续原三轮总预算，不使用联调训练权重。

写盘故障、轮换、首步恢复、连续中断、暂存忽略和终轮续接检查均通过。完整回归 **131 项通过**，独立代码复审无剩余启动阻断。测试日志在 `outputs/framework_restart_2026_09_12/full_tests_release.log`。这些代码检查不替代实际 7B 保存/重载验证。

## 加速实测

所有测试使用现有 RTX 4090、安装的 PyTorch 2.1.2/CUDA 11.8 和本机原模型/状态。原训练已经使用 BF16、FlashAttention 2、梯度检查点。原/候选前后向比较使用同组真实 8 条训练行、相同 RNG，各一次热身、三次计时，没有优化器更新；保留完整上下文和原目标。固定 loss 绝对差 0.001、整体梯度相对 L2 0.01、单参数张量 0.05，并要求原路径重复也满足门槛。

| 候选 | 原用时 | 候选用时 | 数值核查 | 决定 |
| --- | ---: | ---: | --- | --- |
| 仅回答位置计算词表输出 | 12.0099 秒/批前后向 | 11.8938 秒 | 通过 | 仅约 1%，不足预先 3% 门槛，不启用 |
| reentrant 梯度检查点执行 | 12.0081 秒/批前后向 | 12.0548 秒 | 通过 | 没有提速，不启用 |
| fused AdamW 内核 | 0.02540 秒/次更新 | 0.05123 秒 | 参数与矩状态通过 | 更慢，不启用 |

AdamW 表项是独立内核测试，使用实际保存的 452 个张量、340795392 个参数的矩状态/形状、合成参数初值及固定重复梯度；不是完整模型更新速度。其余两项是原 7B 的真实前后向，不含完整运行的保存和验证时间。reentrant 的首次尝试因缺少外部资源环境变量在模型加载前失败，失败目录保留；完整继承原资源后在新目录完成表中结果。

因此本次没有宣称大幅加速，也没有更换模型、缩短上下文、删减训练样本、降低有效批量或缩短三轮预算。保留原 BF16/FlashAttention 2 配置，跳过已经完成的数据准备，并补齐进度保护。原训练最后 100 步约 12.58 秒/步可作历史参照；新训练的实际速度与保存成本另行记录，不能用单一批次基准替代整轮 ETA。

基准和脚本保存在 `outputs/framework_restart_2026_09_12/benchmark_head`、`benchmark_checkpoint_mode_v2`、`optimizer_performance.json` 及对应日志。所有候选均未通过速度门槛，正式重跑不启用它们。

## 本次运行

2026-09-12 11:28（UTC+8）启动，管理 PID 3906、首步子进程初始 PID 3907；冻结 src/scripts/tests/vendor 与启动时工作区逐文件一致。完整数据为 14935 条训练行、540 条验证输入和独立的 540 条验证损失行，研究角色/输入/离线标签仍按原协议。原 GoT 初始化、MTR 冻结、direct 六点任务，有效批量 8、seed20、AdamW 学习率 2e-5、裁剪 1、最多三轮。没有真实 RGB 输入或学习查询器更新。

启动时实际第 1 步保存及重载尚在执行；此处不将启动安排写成完成。实时读取新运行 `status.json`、`first_step/result.json`、`training/resume_verification.json`、`training/progress.json`、`training/latest_checkpoint.json` 并核对实际进程。必须看到实际恢复核查通过和后续优化步骤，才能确认正式首步恢复成功。

## 真实首步保存和重载已完成

11:29，正式第 1 步的完整检查点成功发布，大小约 4.15 GB，保存耗时 5.37 秒；首步 loss 与原中断运行的第 1 步完全一致（0.92366794）。首步进程正常退出，管理器随后重启训练子进程，从该检查点恢复。

11:34 核对 `training/resume_verification.json`：adapter、projector、AdamW 与 CPU/CUDA/Python/NumPy RNG 全部逐项一致。恢复后从 step 2/next_row 8 继续，已完成到第 21 步/168 条样本，更新序列连续、损失和梯度范数有限，恢复后的实际速度约 12.35 秒/步。保存与重载已实际执行，GPU 反向跨运行仍可能非确定，不将状态精确恢复写成之后所有参数更新逐位重复。

证据在 `outputs/framework_restart_2026_09_12/first_checkpoint_check.json`、`resume_and_progress_check.json` 及新训练原始恢复报告。第 25 步周期保存仍待当时进度触发；未完成本轮验证或轨迹质量评价。

## 第一份周期检查点也已独立核对

11:35 第 25 步周期保存完成，约 4.71 秒；文件位于 `outputs/framework_baseline_restart_v1/training/recovery-step000025`。11:36 用独立 CPU 进程读回 training_state.pt、adapter 和 projector：状态 JSON 与二进制中的状态一致，下一行 200，452 组 AdamW 状态的 step 全部为 25，参数和矩状态数值均有限。训练没有为此次检查暂停，已继续到第 34 步。原始证据在 `periodic_checkpoint_check.json`。

当前已有首步与 step25 两份实际完整状态。按当前每份约 4.15 GB 及保留策略计算，本次运行的检查点总量峰值约六份（首步、一轮写入过渡的周期点及轮末点），现有空间估计还余约 2.82 GB；该估计不含其他任务后续写盘。此前独立报告在 step25 正在写入时仍按五份额外空间估算，重复计入了正在保存的部分，已在周期检查报告中按已完成份数更正。没有删除历史数据或检查点。
