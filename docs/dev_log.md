# ToolV2X 开发日志

历史实现与实验见 `STATUS.md` 及各阶段报告。本文件从原 MTR 因果适配阶段开始记录。

## 2026-09-10 原 MTR 适配

- 用户已授权开始标签、评价、原损失训练和适配前后比较，计划见 `mtr_adaptation_plan.md`。
- 已核查原损失所需的中心/邻居未来字段及设备处理；尚未生成本阶段标签或更新参数。
- 本地具备原权重、GT/位姿和 CUDA 环境，无需下载资源。
- 已新增独立匹配/未来标签与原 batch 转换、预测指标模块；5 项回归测试先在缺失实现时失败，正在验证具体数值和有效位。
- 上述 5 项检查已通过；新增 `prepare_mtr.py` 将标签和在线窗口物理分开，记录每个片段、来源和状态的覆盖。当前进入真实数据准备。
- 已扩展原模型设备选择和按中心批量推理；原损失仅改为跟随预测张量设备，公式保持。真实原模型反向传播/更新/批量一致性测试先确认缺少设备入口导致失败，正在执行修复后验证。
- 原模型反向传播/更新及原适配器 5 项检查通过。真实标签准备完成：训练 6,054 个来源帧、63,573 个在线目标，验证 216 个来源帧、4,340 个目标；标签只存于独立离线目录。
- 新增严格按目标身份和标签覆盖配对的指标，先观察缺失入口失败，再实现。新增 `train_mtr.py`，按原配置使用每来源帧所有监督中心、原 AdamW 与衰减，保存运行代码、原始/适配预测、逐轮验证和检查点重载证据。
- 6 项标签/预测指标检查通过，真实 CUDA 最大监督中心帧的原损失前后向成功。独立标签审计复核全部计数并抽查 102 个来源帧、306 个未来帧，2,101 个有效点的最大坐标差 0.000008 米，未读测试标签。
- 正式运行目录 `outputs/mtr_adaptation_v1`：已保存训练代码快照并开始原权重验证与适配。原权重完整上下文验证组平均 top-1 ADE5 为 21.5195 米。
- 新增已有 Q8/Q9 输出评价入口及 3 项数值/失败测试；从真实原点和当前速度计算初始跳变，重复点本身不判失败。轻量审查新增无模型依赖的标签和评价测试。
- 本轮完整集成 82 项、无模型依赖审查 63 项通过。已评价三个旧运行的 12 个动作回答，实际只有两个不同决策帧；结果见 `planning_quality.md`，包含五点回答失败与重复远点轨迹的初始跳变。MTR 正式训练仍在运行，最终质量报告待检查点选定并重载核对后写入。
- 2026-09-11：正式运行完成 10 轮、9,960 步，选中第 5 轮；原始/适配验证组平均 top-1 ADE5 为 21.519 → 5.874 米，检查点重载后指标差为 0。新增 `verify_mtr_adaptation.py` 独立公式复算前后各 7,256 条记录、验证分组、步数与选模；真实选中权重的 456 个参数张量已改变，审计通过。
- 现有运行入口以新权重完成 P/F 工具与原 projector 的真实 prepare 重放，30 个邻车目标的完整 P 本地重算与 F 预测/分数完全一致；没有新 7B 生成。当前 JSON 预算保留 Ego/P/F/PF 记录数为 5/3/5/4。
- 结果、训练波动、完整时域分母、逐目标变差比例与模态变化已写入 `mtr_adaptation_results.md`，图和补充统计由 `outputs/mtr_analysis_v1/analyze.py` 生成。更新 README、STATUS、原来源和审查入口；历史记录保留各自的完成边界。本轮改动未提交或推送。

## 运行说明

使用 `/root/autodl-tmp/conda-envs/llava/bin/python`，设置 `OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1`。本阶段可执行入口与实际运行结果随实现验证更新；当前原推理及审查命令仍见 README。

标签准备：`python src/prediction/prepare_mtr.py outputs/mtr_supervision_v1`。输出目录必须不存在；只准备既定 train/validation。

## 2026-09-11 MTR 稳定性对照

- 已读取训练代码、实际逐轮指标和既有框架设计。旧完整/ROI 每轮分别 1,212/780 次更新，不能把相同轮数当成相同训练量。先在 `mtr_stability_plan.md` 固定两方案、三个种子、每轮 996 步与冻结规则，再改训练入口。
- `docs/user_requirements.md` 已记录用户本次继续授权。现有工作分支与未提交成果保留，不移动或提交用户审查文档。
- `tests/test_mtr_schedule.py` 新增三个行为检查：ROI 剔除/空标签下仍达到相同更新数、保留默认遍历与种子顺序、空监督池及时失败。先运行缺失入口的失败版本。
- 上述三个检查均先因缺少固定更新迭代器失败。`src/prediction/train_mtr.py` 增加原有训练循环复用的 `epoch_examples`、种子/上下文/每轮更新数参数；默认仍每轮遍历一次。协议随每次运行归档，实际更新数不足时明确失败。
- 新测试首次运行暴露空未来标签夹具不符合既有 50 步契约，已改为 50 个缺失帧；原 MTR 真实反向传播检查通过。没有更改生产标签语义。
- `tests/verify_mtr_adaptation.py` 增加固定步数/上下文核对、检查点 epoch/it 核对；初始化被选中时允许并验证权重未变，选模分数按原 1e-6 容差检查。
- 新训练调度检查与完整集成共 85 项通过，轻量审查 63 项通过，`git diff --check` 无问题。`outputs/mtr_stability_v1/run_study.py` 顺序执行六次原网络训练，每次结束立即独立复算，失败时停止并保留原日志。
- 新增本次运行专用 `outputs/mtr_stability_v1/analyze.py`，汇总所有种子/末期结果、核对等预算与相同验证目标，并按预登记规则生成冻结记录和报告；六组全部完成前不生成完成报告。
- `outputs/mtr_stability_v1/expected_sampling.json` 根据相同训练索引/随机顺序重建预期采样计数，已结束轮次计数一致。三种子完整上下文预计监督中心数为 72402/72771/72894，交替为 55581/55491/55579；这是调度重建，未冒充独立记录的逐步输入日志。STATUS 已标明本项正在执行。
- MTR 顺序训练期间，已核对驾驶模型加载、原 LoRA 脚本和现有监督/评价调用路径。`driving_decoder_plan.md` 先固定直接轨迹入口、相同证据预算与旧 Q8→Q9 约束，再补对应实现。
- `tests/test_direct_planning.py` 新增直接提示、单目标监督、Q8 不适用/失败分母和相同证据下单次生成的检查。生成边界替身仅用于接口测试，不写入实验成果。
- 四项直接解码检查先因缺少提示/模式/评价支持失败。`inputs.py` 增加显式 Trajectory 提示，`v2vgot.py` 在同一证据筛选后选择一阶段或两阶段，`run_connection.py` 增加解码参数与实际执行标记；旧链条默认行为保留。
- `adaptation_data.py` 为 direct 只生成轨迹监督，`evaluation/planning.py` 明确区分 Q8 不适用和失败，拒绝隐式模式切换或带父回答的 direct。`scripts/check_review.py` 纳入三个轻量检查，AGENTS 的父回答规则同步限定到已批准的两阶段模式。
- 直接解码及相邻回归共 25 项通过。原 GoT + 首轮适配 MTR 已在一个真实验证帧完成 direct 四动作生成，全部输出保留；这只是新接口接入，不用于初始化/解码选型。两阶段同帧对照正在执行。
- `tests/test_review_fixes.py` 补充独立候选 adapter 目录检查，为加载两个已存在的初始化权重准备；原默认目录的冲突保护继续保留。
- 独立目录检查先因参数缺失失败；`v2vgot.py` 增加可选 adapter 包装目录，`run_connection.py` 为每次运行使用独立目录。原 GoT 两阶段同帧四动作也已实际生成完成，原回答保留。

### 2026-09-11：驾驶端候选目录的真实加载修正

离线 V2V-LLM 原生输入诊断实际触发加载错误：独立包装目录名不含 llava 时，原 builder 误入 AutoModel 分支。接口已固定为原 LLaVA + LoRA，因此显式传入这一类型，避免由存放目录名推断模型类。先记录协议与失败，再修改调用；两类原权重文件及在线 ego-only 隔离规则不变。使用同一诊断目录重跑真实加载及 LoRA 合并数值核对。

### 2026-09-11：共享驾驶参数更新检查入口

按 driving_training_readiness_plan.md 实现一帧四动作累计的一次真实更新检查。原 LLaVA builder 增加显式 trainable_lora，BF16 加载且保留发布 LoRA；推理默认仍合并。只开放 LoRA 与实际浅层 projector，FP32 参数/AdamW，单独保存 loss、梯度、冻结参数检查和参数变化。加载回归先观察缺少 trainable_lora 的失败，再实现。旧 GoT 父回答与旧 MTR 证据仅用于入口检查，正式 2×2 训练仍须使用冻结能力和候选自身生成的父回答。

### 2026-09-11：真实训练入口显存失败定位

GoT direct 原生监督 backward 已通过 Ego 行，但较长 P 行在默认 SDPA 回退路径分配 7.09 GiB 时 OOM，未发生 optimizer 更新。强制内置 Flash 的诊断明确报告 torch 2.1.2 对 sm89/head_dim128 训练不支持；保留两个新目录和失败日志。为下一次检查准备原 train_mem.py 所用 FlashAttention 2，暂不缩短证据或改任务。主入口补存失败类型，并在 optimizer.step 后立即记录实际步数，防止后续保存失败掩盖已发生的更新。

### 2026-09-11：驾驶四组合诊断与独立复算

完成 GoT/LLM × direct/Q8→Q9 的同一真实验证帧16次动作尝试，原文与失败均保留，报告 driving_decoder_results.md。独立核对ego数组、逐动作证据/预算、响应原始字节及请求编码；8条可解析轨迹的float64 ADE3/FDE3复算差为0。GoT两种模式各4/4可解析，LLM均0/4；只是未适配单帧诊断。LLM抽查三层各128行与显式LoRA合并完全一致，8个projector张量一致，原生短提示/双车诊断仍失败，根因未完全确定。当前完整91项、轻量66项通过；真实训练长序列backward仍另有已记录的内核/显存失败，不把测试PASS当作训练完成。

### 2026-09-11：训练使用原生 FlashAttention 2 路径

记录 SDPA 长序列反向失败及内置 Flash 不支持 sm89/head_dim128 的明确报错后，按原 train_mem.py 将 trainable_lora 加载指定为 flash_attention_2；普通推理继续使用SDPA。先增加并观察内核选择断言失败，再修改调用，核对测试通过后等待资源构建和原模型实测。模型结构、证据、标签和损失不变。资源子agent按用户授权从PyPI固定版本源码构建，不访问用户承担下载的域名。

### 2026-09-11：参数保存与独立重载验证

一次更新检查增加保存文件与内存中全部LoRA/projector张量的逐元素核对；独立 verify_driving_update.py 走默认FP16合并推理入口，检查抽样语言权重的显式base+LoRA结果及全部projector，再执行一条真实保存提示的生成。测试生成保留原文，不据格式或误差筛选通过；PASS只表示序列化/重载核对及生成实际执行。未保存AdamW续训状态，不作为可恢复正式训练检查点。

### 2026-09-11：按用户要求先更新 GitHub

整理本轮源码、审查修复、MTR首轮结果、稳定性发布进度快照和驾驶16次真实接入证据。大权重、完整数据、源码快照及用户两份原始审查笔记继续留本机。独立只读代码审查未发现有证据的阻断/重要问题；发布前完整91项和轻量66项再次通过，尚待导出副本检查与远端更新确认。用户已授权更新GitHub，不将后台六组实验或驾驶内核准备的进行中状态写成完成。

发布前核验补充：实际暂存树导出副本在外部资源根目录设为不可用时，66项轻量检查通过，且入口确认未导入torch/transformers；使用已有Python环境，不宣称重建了新依赖环境。源码/精选证据检查未包含模型或数组二进制，凭据模式扫描无匹配。GitHub已确认目标仓库写入权限，准备提交并进行普通快进推送。

### 2026-09-11：GitHub推送后发现并保留中断的MTR尝试

GitHub main及审查分支已普通快进推送并核对远端与本地提交一致。随后实时进程核对发现原runner和MTR子进程消失，第5组full_seed22仅落盘第1轮996步，未有completion，cgroup无OOM；确切中断原因与落盘后额外步数未知。完整保留4组完成/审计产物及第5组中断目录，剩余seed22两方案从原始初始化重跑，不拼接部分训练、不按早期指标换种子。新runner脱离终端启动并记录状态，发布快照的运行状态另作更正。

### 2026-09-11：六组训练全部完成并按既定规则冻结

剩余两组从原始初始化完成 10 轮及逐组独立审计，六组共 59,760 次完整运行更新。执行 analyze.py 生成完整曲线、比较和冻结记录；另用标准库从录制组数值独立复算末三轮均值及选择门槛，结果一致。full 合格，alternating 因 seed20 末三轮均值 21.602 米未低于原初始化 21.519 米而不合格；冻结预先指定的 full/seed20 第 9 轮，验证 ADE5 为 4.841 米。README 与 STATUS 更新完成状态，mtr_stability_results.md 保留曲线波动、选模和训练量限制。已完成候选权重 P/F 各数组一致检查，27 个原 MTR 目标、3 个短历史回退；该检查不参与选模、未执行驾驶生成。

### 2026-09-11：完成训练内核构建并准备四组合真实更新

资源子 agent 从 PyPI 源码构建 FlashAttention 2.5.9.post1 的 head_dim128 限定 wheel，root 已检查 ZIP 完整性、依赖和 API 拒绝其他维度的补丁。driving_training_readiness_plan.md 先补充构建范围、安装方式和旧失败保存要求。随后在既有模型环境仅安装本地 wheel，使用独立运行目录按既定一帧四动作协议检查真实更新和重载；结果以实际运行报告为准。

### 2026-09-11：四组合真实更新与独立重载完成

新增运行专用 run_flash_checks.py 顺序执行四组合，新目录和每阶段日志均保留；每组各一次 AdamW 更新、452 个张量发生变化，全部保存文件与内存中参数一致。独立重载每组抽查三层共 1,572,864 个合并值，误差为 0，8 个 projector 参数一致，并实际生成 PF 的保存训练提示。新增 summarize.py 核对真实训练索引、输入/目标一致性、参数记录和源码快照；首次汇总因手写 sample_id 漏掉原记录前缀而失败，已对照 train 索引修正完整标识后通过，没有改运行数据。报告 driving_training_readiness_results.md、README、STATUS 更新完成边界。

独立内核脚本 independent_reference_check.py 在主环境比较 FP32 显式 attention 与 FP16/BF16 的 causal 固定长度/varlen 输出和梯度，通过固定容差。依赖版本逐项不变；安装后完整 91 项、轻量 66 项检查通过。这些一次更新使用旧训练证据和旧 GoT 父回答，不视为正式共享驾驶适配或候选选型。

### 2026-09-11：完成结果的独立审查与精选发布

独立只读审查重新核对 60 份逐轮报表、六组目标身份/预算和四组更新/重载记录，没有阻断或重要问题。依建议将结果文档的“三个语言层”更正为“三个参数矩阵，涉及第 0 和第 31 层”；上述历史日志中的“三层”也按此理解。更新 GitHub 审查入口、发布说明和原来源记录；.gitignore 只增加实际结果文本、图、内核检查及补丁，不纳入权重/数组/编译二进制或用户原始审查笔记。

原资源补丁含统一 diff 的空白上下文，会触发新增文件的空白检查，保留原文件另生成零上下文 replay_build_patch.diff。已在原 PyPI 源文件的临时副本应用补丁，并逐字核对恢复了真实构建的 setup.py 与 flash_api.cpp；原 LICENSE/AUTHORS 原样复制。未改已编译内核或重跑实验。

本次发布前，暂存文件导出副本在所有外部资源根目录禁用时再次通过 66 项轻量检查；使用既有 Python 3.8 环境，没有新建依赖环境。已保存完整 91 项检查日志、独立审查记录和精选文件扫描结果。
# 2026-09-11：64 帧数据和证据核查启动

按用户“需要的话就先验证”的授权，先写 `training_evidence_audit_plan.md` 并补充 `user_requirements.md`。规则固定为八训练录制组各八帧，抽样只读当前自车密度与时间；通过现有 prepare 执行冻结 MTR 四动作，两个原驾驶初始化生成 Q8，离线独立复算标签并画图。计划新增两个复用现有接口的脚本和少量有意义的统计/抽样测试；尚未生成本轮结果，不将已有单帧结论外推。

新增 `tests/test_evidence_audit.py`，先观察脚本不存在时失败；新增 `scripts/audit_training_evidence.py` 后三项测试通过，覆盖确定抽样、失败不替换及 PF 唯一邻车计数。执行前核对原 loader 默认参数的导入时绑定，改为在导入 loader 之前设置冻结权重；每帧实际加载记录仍须与冻结路径一致。脚本复用 prepare 和原驾驶生成，在线进程不导入离线标签模块。

新增 `scripts/report_training_evidence.py`：离线进程用线性方程求解独立投影历史及六时刻未来，检查原生标签语义、文本目标覆盖和真实父回答预算；生成检测/跟踪鸟瞰图与中文 HTML。测试增加旋转/平移坐标核对及未来时间拒绝，`scripts/check_review.py` 纳入这五项不依赖模型的检查。运行开始后的离线分析源码另存副本，不修改正在运行的在线脚本。

64 帧准备全部完成，开始两个初始化的 Q8。离线数值核对已落盘，首次画图发现 Matplotlib 缓存无法识别系统列出的 Noto 字体；单独验证显式注册已有文泉驿字体可识别后修正绘图字体，并保存首次图/源码再重绘。模型数据与生成流程不受此显示问题影响。

独立审查发现静止定位抖动被路径 atan2 统计误称转向：g2762 三秒净位移仅 0.02294 米，原始切线累计值却为 626.3 度。已用新增失败测试确认无有效性门控，再引入离线位移门槛与真实 pose 航向展示；六项测试通过。当前 64 帧、Q8 输入、标签均不改。该帧两源跟踪为空而原始检测各 11 个，继续独立定位到相邻 pose 的 67 米跳变，计划追加只读连续性范围核对并保存证据。

只读连续性补查在 3912 个训练片段内相邻 pose 间隔中发现一处超过记录阈值，影响驾驶三秒标签或一秒历史范围内 40 个训练候选帧；固定抽样仍为 64 帧。`report_training_evidence.py` 另补近自车原点 1.5 米候选的几何敏感性统计，59 帧每个协作动作保留该区域目标，PF 有两条记录，不确认身份或用途。中文图已按修正条件重绘，初次图/数值/源码均保存。全套 97 项、轻量 72 项通过；开始编写 `training_evidence_audit_results.md`，最终 Q8 计数仍待两初始化运行完成后填入。

空轨迹帧还暴露旧 `run_connection.prepare` 无条件把 actual_original_cmp_executed 写成 true。独立核对 C.predict 在零目标时不进入 forward；本轮 320 次接口调用中 315 次有原模型目标，63 帧实际发生原 MTR 前向，一帧两源均为空。修正该执行元数据为基于真实 model_used 计数，并增加处理目标次数；不改变预测、证据或选择规则。旧 64 帧文件保留，另用原空帧做真实 prepare 回归，确认新元数据和四动作证据一致性。

### 2026-09-11：64 帧验证全部完成

两初始化均完成 256 次实际 Q8，无执行异常；GoT 256 条可解析但 254 条为 fast/straight，V2V-LLM 256 条均因格式无效而不能构建合法 Q9 父回答。GoT 的 256 个真实父提示及标签监督长度均在预算内；LLM 对应计数为未检查，不写成预算通过。未来标签只在离线分析进程读取；此次没有 Q9 生成或参数更新。

新增运行专用 `verify_final.py` 独立复核全 64 帧原始数组、512 行容量账本与 512 条回答；验证结果写入 `verification.json`。12 张中文场景图、可展开原文的 HTML 和总览图均已生成并目视检查。完整 97 项、轻量 72 项在元数据修复后再次通过，两轮独立审查没有未解决的重要代码问题。`training_evidence_audit_results.md`、`README.md`、`STATUS.md` 更新了完成范围和下一步：先核实/处理位姿时间断点及证据接收，固定自己的 Q8 父回答和公平轨迹监督预算，再进入正式驾驶适配。原模型运行、异常样本及初次显示/转向诊断版本都保留。

### 2026-09-11：收缩方案第一步——Ego / 固定 F 配对样本

用户要求“开始下一步”，本次执行配对样本准备。目的：为同一驾驶器上的输入增量比较提供可训练、可复用的数据。原按混合目标距离截取会使 F 挤掉 Ego；compact 的共享字段还会随远端目标改变，因此需分别编码本车和远端两个证据块，先在无远端读取的情况下固定 Ego，再追加 F。沿用两位小数及全部六模态，不另做表示选型。

首版总上下文 4096、轨迹生成预留 256，固定给追加远端块预留 1536 token；本车提示连同实际特征 token 最多 2304。该分配用于保持本车输入一致，未按成绩优化，会限制纯 Ego 的文字容量，不能据此声称充分优化的单车上界。保留完整/入模记录与实际成本，之后若容量确实影响当前结论，再局部调整。

数据沿用现有训练/验证录制组。单独离线扫描这些片段的 pose 连续性，沿用已记录的每相邻帧大于 20 米或 20 度异常阈值；统一停用一秒历史或三秒标签跨断点的窗口，记录每个排除理由。它是可审查的异常隔离规则，未核实真实传感器时间，也不修复冻结 MTR 既往训练来源。在线物化进程只接收保留后的因果索引，F 只在实际查询后读取邻车窗口；离线进程最后连接既有标签。

实现范围：现有提示及 direct 监督接口增加可选远端块；新增一个在线配对物化入口和一个离线准备/汇总脚本。模型、tokenizer 按运行加载一次，原 MTR 使用已有目标批量参数，避免逐帧重复加载。输出应能直接进入后续共享驾驶训练和配对评价；本次不训练驾驶模型或查询器。

先加入 `test_paired_driving.py` 并实际观察四项预期失败，覆盖文字块隔离、断点边界、direct 监督和真实 tokenizer 容量。随后实现可选远端提示块、direct 监督透传及 `paired_inputs.py`；复用 `load_window` 的元数据检查，并允许调用者缓存已读取的两个源归档。新在线入口先落盘 Ego，再查询 F，保存原预测数组、报文、固定本车块及追加块。首帧比较现有 CPU 单目标与本次 GPU 批量参考算子输出，预先采用 rtol=1e-4、轨迹/GMM atol=.01、分数 atol=1e-5；此项不代表原生 CUDA 算子一致性。

新增离线 `prepare_paired_driving.py`：index 阶段输出断点隔离清单及因果索引；finalize 阶段在在线物化结束后连接既有标签，逐帧重新计算六点监督、核对所有原特征数组、字节计数、上下文预算和 assistant-only 损失标签，导出配对训练行与无标签的验证输入。轻量审查入口纳入三项纯数据/提示检查。尚待测试及全量真实运行，不标记数据已完成。

四项新增检查通过，完整 101 项、轻量 75 项均通过。离线 index 保留 2987/3027 个训练帧、108/108 个验证帧；训练仍覆盖 8 个录制组，验证 2 个，唯一异常仍为 g2760→2761，停用 40 个窗口，清单在 `outputs/paired_driving_data_v1/continuity.json`。随后启动 GPU 批量预测物化，首个真实帧的 CPU/GPU 参考执行比较已通过预定容差。测试日志已复制到新运行目录；全量证据及训练行尚待完成。

### 本轮运行说明

使用既有 llava 环境，依次执行以下三个入口。index 必须使用新目录；在线阶段保存真实 MTR、F 报文和配对提示，finalize 只有全部帧完成后才导出训练行。三个过程顺序执行，在线过程不导入离线标签模块。

```bash
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 PYTHONDONTWRITEBYTECODE=1 /root/autodl-tmp/conda-envs/llava/bin/python scripts/prepare_paired_driving.py index outputs/paired_driving_data_replay
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 PYTHONDONTWRITEBYTECODE=1 /root/autodl-tmp/conda-envs/llava/bin/python src/planning/paired_inputs.py outputs/paired_driving_data_replay
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 PYTHONDONTWRITEBYTECODE=1 /root/autodl-tmp/conda-envs/llava/bin/python scripts/prepare_paired_driving.py finalize outputs/paired_driving_data_replay
```

### 2026-09-11：配对数据准备完成

在线进程正常完成 3095 帧，耗时约 858 秒；没有驾驶参数更新或 Q9 生成。离线进程逐帧重新计算原生标签、比较保存的全部本车特征数组、核对文字块、报文字节和预算，并生成 5974 条 assistant-only 训练行及 216 条不含答案的验证输入，正常退出。另以运行目录中的标准库 `verify_export.py` 独立读取导出文件，确认每帧恰有 Ego/F 两行、共享特征与同标签、训练验证分离以及 40 个停用窗口未进入样本，PASS。当前在线源码与保存快照逐文件一致。

本步判断：本车与邻车分别编码解决了比较中的输入变化问题；原模型和监督接口可复用。文字容量仍限制可见内容，训练平均本车 10.37→2.49、邻车 10.41→2.96 条记录，验证邻车 20.91→2.57。四帧 F 无目标返回也保留，返回却全被截断为零。当前固定此版本推进一次共享驾驶适配；后续负结果需结合覆盖解释，未因容量限制另开表示研究。报告 `paired_driving_data.md` 及 README、STATUS、当前方案同步完成状态和论文论断边界；测试仍为本次完整 101 项、轻量 75 项通过，后续仅修改文档，未重复扩大测试。

### 2026-09-11：根据用户纠正，改为补齐完整框架

用户说明“基础”指参考他人的完整框架，避免整体仍未完成时持续做局部数据；不是只完成 Ego。此次只核对论文与代码并修订工作顺序，没有进行模型训练、生成、数据重建或资源下载。

目的与理由：此前把下一项交付压缩成固定 F 轨迹表，之后又误缩成 Ego 训练，都没有回答完整系统如何运行的问题。复核 DriveAgent-R1 v3 和 V2V-GoT v4 的正文及原设计，确认它们对任务输入、运行流程、训练数据和评价有成套安排；ToolV2X 已有组件复用路径，但工具决策过程和统一训练/运行仍缺实现。

优化取舍与一致性：`run_connection.py` 按外部预定动作调用工具，尚无依据实际结果再次选择与停止的执行器；`V2VGoTPlanner.plan` 仍用混合 `fit_evidence`，配对数据使用独立本车/邻车块，正式训练前需统一。完整链路覆盖固定对照及简单规则调度，仍复用原 GoT/MTR，不增加骨干矩阵或提前确定 RL。

后续与论文价值：原始因果观测、MTR 数组、工具报文和六点标签继续可用；现成 5974 条轨迹训练行没有调用过程监督，不能代表完整 agent 训练数据。新增 `docs/framework_completeness_review_2026_09_11.md`，同步需求、README、STATUS 和旧收缩方案的优先级说明。下一项工作围绕整体实现规格及关键运行链，不自动启动单独 Ego 训练或固定 F 增益实验。新文档属于框架审查和顺序修订，尚未产生新的实现或方法性能证据。

### 2026-09-11：开始实现完整运行基线

用户已授权执行整体框架修订。按 `framework_implementation_v1.md` 统一输入构造、工具决策循环、训练与评价；沿用现有工作分支，保留未提交改动。先验证基线，再加入有因果与输入一致性意义的回归检查。设计选择使用同一执行器承载固定对照和明确规则；P 本地重算保留默认，可选纯观测接收。实现规格说明数据和训练接口、单一基础适配设置及其证据边界，当前尚未产生新运行结果。

### 公共输入与任务循环：首版实现

基线 101 项测试通过；新增 7 项回归全部因缺失功能而失败。已将原配对选择函数移入 `planning/context.py` 并保留旧导入；新增公共 `build_plan_input`，驾驶器增加 `plan_prepared` 和明确布局入口，避免生成前再次选择已训练输入。`VehicleTools` 接收器增加显式纯观测 P 选项，默认仍运行已有本地 MTR。新增 `episode.py`，统一固定动作和规则策略的实际查询、结果更新与停止；本地 P 计算与远端服务分开计数，任务内只复用已收到的 P 窗口。以上尚待回归验证，不标为完成。

### 真实任务入口与离线监督导出

公共输入与任务循环的 7 项检查通过，包括真实 tokenizer 对旧 Ego/F 提示的精确重建。新增 `run_framework.py` 直接执行冻结 MTR、真实 P/F 与规则/固定循环，再生成公共驾驶输入；生成子命令对已实际查询的任务加载原驾驶器，在线不读取标签。新增独立 `prepare_framework_training.py`，核对录制角色与帧号后导出监督和独立验证输入。新增流程测试先观察 3 项预期失败；训练/评价模块继续实现。

### 训练与统一评价接口：首版实现

新增 `train_driver.py`，复用原模型前向和可训练参数，提供多样本训练、按录制/策略汇总的验证损失、模型与优化器/RNG 状态保存和继续训练；训练开始时重新校验提示与实际在线任务一致。新增 `evaluation/framework.py`，把调用字节、终止原因、失败分母和配对轨迹误差连接到同一实际任务，另报简单运动外推。新增 3 项离线流程测试之前已观察预期失败，现继续验证；当前没有宣布正式适配完成。

### 真实任务与分组修正

首批调试按 scene 片段采样得到 34 帧/170 任务，发现与“每录制组两帧”的描述不一致，完整保留 `framework_debug_v1`。新增跨片段录制组采样测试并观察失败，改用既有 `recording()` 聚合抽样、验证损失与逐组评价；修正后 `framework_debug_v2` 的 20 帧/100 个真实任务正常完成，用时约 32.6 秒。完整 112 项检查通过，正式训练前还在进行独立代码审查。新训练入口现对 80 条训练/20 条验证输入进行两步参数更新、保存与后续续训/生成联调；这是完整框架的集成验证，不标为正式适配收敛。

### 2026-09-11：完整联调、独立审查修复与全量任务启动

用户连续要求“继续”，按已授权整体框架推进。独立审查发现两项 P1：接收处理异常会丢掉已发生的工具账本；不完整生成目录会被评价为完整。新增故障注入测试观察 3 项失败后修复：任务及请求先落盘，响应/成本在接收更新前保存，失败保留身份与未知成本标记；准备和生成分别核对完整预期任务集合，评价缺记录时拒绝发布完成，监督导出拒绝忽略失败。独立复审确认两项关闭。

新训练入口完成真实 2 步保存、恢复第 3 步，再恢复完成全部 80 条训练行（共 10 步）和 20 条验证行。LoRA/projector、优化器、CPU/CUDA/Python/NumPy 随机状态逐项恢复一致，记录在 `framework_training_epoch_v1/resume_verification.json`；独立连续 3 步运行在恢复前已有反向非确定性，不写成恢复错误或完全可重复的 GPU 训练。增量样本计数与已达预算的恢复检查已修正。小样本完整一轮明确 `formal_adaptation_complete=false`，验证宏平均答案损失 0.686655。

重载一轮检查点后对全部预选 4 帧进行五策略生成，20/20 可解析；统一离线评价和独立 ADE/FDE/报文字节/输入核对完成。该联调的 Ego/P/F/PF/rule ADE3 分别为 13.014/15.520/12.639/13.086/29.451 米，简单运动参照 1.175 米，含一条六点重合输出。结果说明质量未达标，不能用来判断协同收益。全部四帧图和结果见 `framework_baseline_results.md`；完整 115 项、轻量 87 项检查通过。

新增标准库顺序运行入口 `scripts/run_framework_baseline.py`，从保存的 src/scripts/tests/vendor 副本启动独立子进程，按 prepare→export→train→generate→evaluate 执行并保存阶段状态。2026-09-11 19:03（UTC+8）启动 `outputs/framework_baseline_v1/`，主进程初始 PID 8392。首批 155 帧/775 任务实际完成、工具失败 0，后续读取实时进度。全量使用既有清理索引的 3095 帧五策略，预期 14935 条训练行和 540 条验证输入，原 GoT 初始化，冻结 MTR，固定一套三轮上限配置；当前训练与后续评价尚未完成。历史产物保留，没有提交或推送 GitHub。

### 2026-09-11：用户要求更新 GitHub，发布前核验

按用户“更新github”整理完整框架、此前未发布的证据核查与配对数据代码/文档，以及约 8 MiB 精选真实文本。上传全部 20 个验证任务与生成、4 条离线标签、2 个训练帧规则提前停止实例；100 项原索引保持完整但其余任务和特征不发布。原始三份审查草稿、权重/数据二进制、重复源码快照继续留在本机。

为网页版审查补入两项真实归档复算，核对统一提示、实际字节、ADE/FDE 和退化轨迹保留。完整 117 项、轻量 89 项通过；Git 暂存内容导出的独立副本在无 PyTorch/Transformers、外部资源不可用的环境中通过 89 项。新增文件范围筛查与暂存差异空白检查通过。远端 main 和开发分支与更新前本地一致，采用正常提交及快进推送；后台 src 与已启动副本逐文件一致，发布操作不变更运行代码。发布时进度保存为固定快照，核验文件位于 `outputs/framework_review_2026_09_11/`。
