# 下一轮数值驾驶训练：降低检查点频率的准备配置

2026-09-15，按用户“每25步保留完整检查点频率过大，可以调大”的要求准备，未执行。

完整配置从最近真实v2预检配置复制，仅修改 `save_interval=500` 和 `validation.interval_batches=500`。原始20轮历史准备包及已执行预检配置均不改写。`fit` 在验证时也保存完整检查点，因此不能只修改保存间隔而留下每100批的验证保存。

初始化、训练结束和显式有界停止仍由原入口保存；如本轮少于500批，会保存开始和结束状态。保留 `epochs=1` 的现有小批预检上限，未重新授权全量20轮，也没有把500批当作训练总步数。数据范围和实际停止步数须在下一次执行记录中绑定；本配置没有自动运行入口。

所有已存在检查点保留。本次未实现最近N份的自动清理策略，也未删除旧权重。配置已通过现有 `_config` 校验，并核对除上述两个数值外与来源完全一致；没有新优化步骤或模型调用。
